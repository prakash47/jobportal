# WORKLOG — Active Development Board

> **The live "who is building what RIGHT NOW" board.** Its only job is to stop two
> developers from building the same table, feature, or component twice — and to
> stop two people from editing the same conflict-prone file at the same time.
>
> **This is NOT PROGRESS.md.**
> - `PROGRESS.md` = what has already **shipped** to `develop` (the permanent log).
> - `WORKLOG.md` = what is **in flight right now** (a short, fast-moving board).
>
> It is a **git-tracked file**, so it syncs to every machine on `git pull` / `git push`.
> Claude Code reads this at the start of every session and **before starting any new
> work** (CLAUDE.md §15). If your planned work overlaps something here, coordinate first.

---

## How to use it (takes 30 seconds)

1. **Start of session / before new work:** `git pull` the latest `develop`, then read this board.
2. **Check for overlap.** Is someone already building the thing you're about to build? Is the file/surface you need to edit locked below? If yes → ping them, pick something else, or wait.
3. **Claim it.** Add a row under **🔨 In Progress** with your name, branch, what you're building (name the DB models / components / endpoints), and which **shared surfaces** you'll touch.
4. **If you'll edit a shared surface** (schema, theme tokens, a barrel file — see the table) → also claim it under **🔒 Shared-surface locks**. Only one person holds a lock at a time.
5. **When merged:** move your row to **✅ Recently merged**, release any lock you held, and record the real entry in `PROGRESS.md`.
6. **Commit the WORKLOG change** together with your work (or as a tiny separate commit). It travels through git like everything else.

> Keep entries short. This board is meant to be glanced at, not read.

---

## 🔒 Shared-surface locks

These files are edited by everyone, so two simultaneous edits = guaranteed merge conflict or duplicate migration. **Hold the lock before you touch them; release it the moment your change is merged.** A lock should last hours, not days — make the change, merge, release.

| Shared surface | File / path | Held by | Branch | Since | Notes |
|---|---|---|---|---|---|
| **DB schema + migrations** | `packages/db/prisma/schema.prisma` (+ `prisma/migrations/`) | Claude/Prakash | `feature/srp-emp-mode-filters` | 2026-08-08 | ⚠️ **COMMENT-ONLY — no model, field, enum or migration change.** One stale line on `enum WorkMode` said the field becomes "Filterable on the public SRP later"; this branch makes it filterable now. Safe to take over if you need the lock — ping me and I'll drop the line. |
| **UI theme tokens** | `packages/ui/src/styles/theme.css` | — free — | | | Released 2026-07-30 after `feature/brand-nav-loader` merged. |
| **Shared types** | `packages/types/src/*` | — free — | | | Zod schemas + shared types. |
| **Web home barrel** | `apps/web/components/home/index.ts` | — free — | | | Append-only; coordinate big rewrites. |
| **UI atoms/molecules barrels** | `packages/ui/src/components/*/index.ts` | — free — | | | Append-only. |
| **Feature flags** | `packages/feature-flags/src/keys.ts` | — free — | | | New flag keys. |

> "Held by: — free —" means anyone can take it. To take it, replace `— free —` with your name + branch + date, commit, push. To release it, set it back to `— free —`.

---

## 🔨 In Progress

> One row per active piece of work. Name the concrete artifacts (models, components, endpoints) so overlap is obvious at a glance.

| Developer | Branch | Building | Shared surfaces |
|---|---|---|---|
| Claude/Prakash | `feature/srp-emp-mode-filters` | **ADR 0002 decision 6 — make the `emp` / `mode` SRP facets actually filter.** Both facet groups render on the live site today and filter nothing: `Job.employmentType` / `Job.workMode` exist in Postgres but are absent from the Elasticsearch `jobs` index, and `parseSrpSearchParams` deliberately drops them. Adds `employmentType` + `workMode` to the ES mapping/transform/query (`packages/search`), maps `emp`/`mode` in `parseSrpSearchParams` (`packages/domain`) — which repairs all five SRP surfaces at once (`/jobs`, city-jobs, skill-city, skill-jobs, `GET /v1/jobs`) — and deletes the two now-false comments plus the user-facing "schema columns land with the recruiter feature" line in `Filters.tsx`. | `packages/search/src/{types,indexes/jobs.index,transforms/job.transform,queries/searchJobs}.ts`, `packages/domain/src/srp-params.ts`, `apps/api/src/public-jobs/dto.ts`, `apps/web/components/srp/Filters.tsx`. **No lock needed** — no schema change (columns already exist → no migration), no `theme.css`, no `packages/types`, no barrels, no `keys.ts`. ⚠️ **Ships an ES mapping change: run `pnpm --filter @jobportal/search search:reindex` on pull.** |

---

## 🅿️ Planned / Up next

> Things claimed but not started yet — so others know they're spoken for.

| Developer | Feature | Notes |
|---|---|---|
| Claude/Prakash | **Admin console migration — PR 3 (`feature/sadmin-admin-migration`)** | Moves feature-flags · audit-log · kyc-review · support out of `apps/web/app/admin` into `/sadmin` and deletes the old subtree. **Must land after PR 2**: `/admin/feature-flags` is the only surface that can turn moderation OFF, and a raw DB update can't substitute (`invalidateFlag()` is only called from `setFlag()`, so the 30s cache keeps serving the stale value). Also takes the `keys.ts` lock for the moderation-flag criticality change. |

---

## ✅ Recently merged (rolling — keep ~last 10; the full permanent log lives in PROGRESS.md)

| Date | Branch | What shipped |
|---|---|---|
| 2026-08-08 | `feature/applications-additive-fields` | **The last endpoint (ADR 0002 step 10)** — two purely additive fields on the existing authenticated `GET /me/applications`, both in `ApplicationsService.list()` so the controller is untouched. **`counts`** is per-status totals across ALL the caller's applications and is **deliberately not filtered by `?status=`**: they drive the filter chips, so deriving them from the filtered page would make every chip show the count of whichever filter is already active. Verified live — `total` moves 19→11→5→0 across `?status=APPLIED/IN_REVIEW/REJECTED` while `counts` holds at `{APPLIED:11,IN_REVIEW:5,SHORTLISTED:2,INTERVIEWED:1,ALL:19}`. **`statusHistory`** is the raw `Json?` column coalesced `null → []`, with a non-array value treated as no history. **For the app: every seeded row returns `[]`**, because those applications predate the column being written — a timeline must synthesise the APPLIED step rather than assume history exists. Backward compatibility re-verified live: every pre-existing key on the hit and its nested job is present and unchanged. Adversarial 4-lens review, **33/33 agents, no errors: 10 raised → 1 confirmed** — the implementation was correct but **the test guarding its central claim was vacuous**: `toMatchObject` is a recursive SUBSET match, so asserting `where: {userId:42}` was satisfied by a received `{userId:42,status:'REJECTED'}`, and a fixed `mockResolvedValue` made the counts assertion argument-blind too. Now an exact `toEqual` plus an argument-sensitive mock, and **proven rather than asserted**: the regression was injected, the test confirmed failing, and the service restored byte-identically. Same class of defect the companies review found — a test whose fixture made it incapable of failing. **API 959 → 968 tests.** Gate green on the integrated state: typecheck **13/13** · tests **10/10** · build **5/5**. **Nothing to run on pull.** |
| 2026-08-08 | `feature/public-career-advice-api` | **Career advice — `GET /v1/career-advice` + `/v1/career-advice/:slug`** (ADR 0002 step 9; `apps/api` only). **Cheap because of owner decision 3**: returns the raw `body` markdown rather than server-rendered HTML, so none of the Shiki/unified ESM-vs-CommonJS problem applies — the branch scoped at **2–4 days took about two hours**. Stated trade-off: sanitisation moves to the client, so the app must render this as MARKDOWN and never as HTML, and syntax highlighting is lost on mobile; `apps/web` keeps its own pipeline untouched. The **PUBLISHED gate is pinned in the service's where clause** (not inherited from the shared parser) and `status` is absent from a `.strict()` DTO — verified live by temporarily drafting a real article: detail **404'd with a body byte-identical to an unknown slug** and the list dropped 3→2 with no leak, then the row was **restored and verified byte-identical**. Two deliberate differences from the siblings: page size **20** vs the web index's 12 (owner decision), and **no slug-drift 308** — `Article.slug` is the whole URL key with no numeric permalink, so a changed slug is a different article, not an alias. Ordering is `publishedAt desc, id desc` because **publishedAt is NULLABLE**, so the tiebreaker is what makes offset pagination deterministic at all. Adversarial 4-lens review, **73/73 agents, no errors: 23 raised → 0 confirmed** — no behaviour defect survived verification, but **two of my comments did not**: `parseFaqs` claimed to be “the same guard the SSR applies” when the web guard is all-or-nothing (`v.every`) and this one filters entry-by-entry, and the `orderBy` comment omitted that Postgres DESC is NULLS FIRST (inherited from the website; 0 such rows today). Both corrected. **API 941 → 959 tests.** Gate green on the integrated state: typecheck **13/13** · tests **10/10** · build **5/5**. **Nothing to run on pull.** |
| 2026-08-08 | `feature/public-home-api` | **Home feed — `GET /v1/home`** (ADR 0002 step 8), **plus owner decision 5, which changes the WEBSITE.** The endpoint is the one genuinely thin piece of this programme: the whole ~10-query aggregate already lived in `@jobportal/domain/home-queries` after the extraction PR, and it is the same function the website's homepage renders from, so the two tabs cannot show different inventory. A composite rather than six calls because this is the cold-start path where latency, not bandwidth, is the cost; `Cache-Control: s-maxage=1800` matches the page's `revalidate`. The web-only `redirect('/profile')` was deliberately **not** ported — that is routing, not a data gate. **Decision 5:** `counts.companies` was an unfiltered `company.count()` shown beside a live job count, reading as “employers you could apply to” while including companies with nothing open; it now counts only companies with ≥1 ACTIVE job, **and the labels moved with it** — hero “companies” → **“companies hiring”**, “hiring teams” → **“recruiters”** (accounts are not teams; several recruiters share one employer), mega-panel “companies listed” → **“companies hiring”**. **Honest note: all 12 seeded companies are hiring, so the live number did NOT move and a browser check cannot distinguish the filtered query from the old one** — a new `packages/domain` suite pins it by asserting the where clause reaches `prisma.company.count`. Adversarial 4-lens review, **70/70 agents, no errors: 22 raised → 1 confirmed HIGH**, also mine: the new test cast its Prisma mock to a `Record`, which under `noUncheckedIndexedAccess` widens every property to `| undefined` — **23 TS18048 errors, turning a green monorepo typecheck red**. Missed because I ran typecheck *before* adding the file and only vitest after, and vitest transpiles without typechecking. Fixed with the house explicit-literal idiom. **domain 81 → 84 tests.** Gate green on the integrated state: typecheck **13/13** · tests **10/10** · build **5/5**. **Nothing to run on pull.** |
| 2026-08-08 | `feature/public-companies-api` | **Companies for mobile** (ADR 0002 step 7) — `GET /v1/companies` + `GET /v1/companies/:handle`. Directory reuses `parseDirectoryParams` and the SSR's exact orderBy ladder (every branch ending in a unique `id` tiebreaker); open-role counts are **one grouped query per page**, never one per card. An unknown category is **ignored, not 404'd**, matching the SSR. Detail ports `loadCompany`/`loadRelatedCompanies` out of the unexported page-file functions, plus openings (ACTIVE, take 10), reviews (take 5) and `isVerified` where an **absent** KYC row is itself the not-verified state. **`parseHighlightSections` MOVED into `@jobportal/domain`** rather than copied — it decides what a valid culture block is, and the two surfaces must not disagree; the web component re-exports it, and all four affected website pages re-verified at 200. Page size **20** here vs the website's **24** (owner decision, deliberate). Adversarial 4-lens review, **46/46 agents, no errors: 14 raised → 5 confirmed**, all one root defect **I introduced**: handles were built from `slugify(name)` instead of the stored `slug` column, which is an independent `@unique` value — so “Tarang Hotels & Resorts” (slug `tarang-hotels`) got the handle `tarang-hotels-resorts-overview-10`, which then **308'd to itself forever** (`curl -L` exit 47). Fixed with a new **`buildCompanyHandle({slug,id})`** in `@jobportal/domain` as the proper pairing for `parseCompanySlug`, so parse and build round-trip; `buildCompanySlug` stays, now documented as the mint-a-new-slug function that is wrong for reading an existing row. **The test that should have caught it used a fixture where `slugify(name)` happened to equal `slug`**, collapsing the two sources of truth — the fixture now deliberately diverges and an anti-loop invariant asserts a redirect target is never the requested handle. Verified live: 0/12 mismatched handles, and the old bad handle resolves in exactly one redirect. **API 919 → 941 tests.** Gate green: typecheck **13/13** · tests **10/10** · build **5/5**. **Nothing to run on pull.** |
| 2026-08-08 | `feature/public-catalogs-api` | **Reference catalogs — `GET /v1/skills`, `/v1/cities`, `/v1/industries`** (ADR 0002 step 6; `apps/api` + a one-line guard in `@jobportal/domain`; no schema/migration/flag/lock). **Promoted ahead of companies** because the spec framed these as filter pickers when they are really a blocker for the app's **profile screen**: `GET /me/profile` returns bare `skillIds`/`preferredCityIds`/`industryId` with no names (verified — the seeded candidate answers `[7,110,62,47,80]` and `[1,4]` and nothing else). So the endpoints carry an **`?ids=` resolve mode** — `?ids=7,110,62,47,80` → “Distributed Systems, Go, Kafka, Kubernetes, PostgreSQL”. Resolve mode ignores `q`/`page` deliberately (paginating a lookup would silently drop ids), de-duplicates, caps at 100, and omits a stale id rather than 404ing the request. `?q=` adds server-side case-insensitive search — a **superset** of the website, whose pickers download the whole table. `pageSize` is client-settable **here and only here** (default 20, cap 100): a deliberate divergence from the feeds' fixed 20, because a picker wants the table in one round trip. Custom skills are **not** filtered, matching the SSR pickers. Adversarial 4-lens review, **67/67 agents, no errors: 21 raised → 1 confirmed HIGH** — and its fix hint turned one instance into **five**: every `id` column is a Prisma `Int`, and a larger value makes Prisma **throw**, not match zero rows, so an anonymous caller produced 500s and Sentry events by adding a digit to a URL. `?ids=2147483648`, `?ids=1e10`, `?ids=3000000000`, the authenticated `jobIds`, **and the already-merged public `/v1/jobs/x-2147483648`** all 500'd; all now answer 400/404 with the boundary value unchanged. The slug fix lives in `@jobportal/domain`, so it repairs the **website and the API together**, and `parseCompanySlug`/`parseWorkingAtSlug` had the identical hole. **domain 78 → 81, API 899 → 919 tests.** Gate green on the integrated state: typecheck **13/13** · tests **10/10** · build **5/5**. **Nothing to run on pull.** |
| 2026-08-08 | `feature/public-jobs-api` | **Public job browse for mobile — the biggest single unblock** (ADR 0002 step 5; `apps/api` only, no schema/migration/flag/lock). **`GET /v1/jobs`** (paginated, ACTIVE-only), **`GET /v1/jobs/:slug`** (optional auth, 308 on slug drift) and **`POST /v1/me/job-state`** (bulk saved/applied — shipped here deliberately because it changes the `/jobs` response shape and versioning it later would cost far more). **Reuse, not reimplementation**: the ES query is `searchJobs` from `@jobportal/search` — the same function the website SRP calls — and params/slug/visibility all route through `@jobportal/domain`, which is exactly what the extraction PR existed for. **Security in two layers**: `status` is absent from a `.strict()` DTO *and* pinned to ACTIVE after the spread, because `searchJobs` only DEFAULTS to ACTIVE and the param is caller-overridable. **Ordering is load-bearing** on detail — the visibility check runs BEFORE the 308, since the redirect's `Location` carries the title-bearing slug; verified against a temporarily-`PENDING_MODERATION` job that a guessed slug returns **404 with no Location header**, byte-identical to an unknown id (DB restored byte-identically after). New **`OptionalJwtAuthGuard`** — non-throwing, so a missing *or malformed* token means anonymous rather than turning a public page into a 401. **Adversarial 5-lens review, 86/86 agents, no errors: 27 raised → 5 confirmed**, all one root defect — `searchJobs` was unhandled, and `@elastic/transport`'s `ResponseError` carries `statusCode`+`message`, the exact duck-type the global filter used, so an ES failure on the API's first unauthenticated route **answered with ES's own status, echoed its raw exception text naming the engine and its internal settings, and was classified as an expected 4xx so Sentry never saw it**. Fixed three ways: the page bound is now DERIVED (`MAX_PAGE = ES_MAX_RESULT_WINDOW / PAGE_SIZE` = 500, was a guessed 1000 that admitted 500 always-failing pages), ES failures become a clean **503** with the original as `cause`, and the filter now requires a **boolean `expose`** to recognise an http-error (verified: http-errors sets it true/false, ES leaves it undefined) — status still adopted for genuine http-errors, message only when exposed. **Verified by actually stopping the `jp-elasticsearch` container**: clean 503, no internals, and the client recovers without an API restart. **API 858 → 899 tests.** Gate green on the integrated state: typecheck **13/13** · tests **10/10** · build **5/5**. **Nothing to run on pull.** |
| 2026-08-08 | `feature/domain-package-extraction` | **New `@jobportal/domain` package — the prerequisite the mobile spec had no phase for** (ADR 0002 decision 2 / step 4). Six of the nine mobile endpoints need rules that lived in `apps/web/lib`, which `apps/api` **structurally cannot import** (no dependency, no path alias, `include` scoped to `src/**`). Built in the spec's order, each endpoint would have copy-pasted what it needed, leaving the slug parser, the visibility rules and the SRP param mapping in two places, drifting from the website forever. Six modules moved with `git mv` (history follows) plus their test files: `slug.ts` · `job-visibility.ts` · `srp-params.ts` · `company-params.ts` · `article-params.ts` · `home-queries.ts`. **Behaviour-preserving — no query, endpoint, route or output changed**, and the invariant proves it: `apps/web` **216 → 138** and the new package **78**, summing to **exactly 216** (no test lost, none duplicated). One module needed real surgery: `home-queries.ts` wrapped its loader in React's `cache()`, which is **RSC-only and inert outside a React request**, so it could not move into a package shared with NestJS — the package exports the **uncached** loader and `apps/web` keeps a thin `cache()` wrapper at the original path, load-bearing because several server components call `loadHomePageData()` in one render. An explicit **`exports` map was required, not just tsconfig paths**: subpath imports typechecked (TS resolves via paths) and then **failed at runtime** (vitest/Node resolve via `package.json`) — caught by the web suite, fixed with named subpaths matching `@jobportal/ui`/`@jobportal/observability`. 22 import sites rewritten rather than shimmed; two barrels (`lib/url/index.ts`, `lib/srp/index.ts`) forward so their consumers are untouched. **Deliberately NOT moved**: `lib/cms/markdown.ts` — ESM-only Shiki against a CommonJS API build, and ADR 0002 decision 3 (raw markdown to the client) removes the reason to share it. Browser-verified on every surface these modules power: home · SRP with filters · companies with category+hiring · career advice with search all **200**; job detail **200**, slug drift **308**, malformed slug **404**, unknown id **404**; company handle **200** and its drift **308**; all 23 SRP job hrefs end in a numeric id per CLAUDE.md §5; **0 console errors**. Adversarial 5-lens review, **65/65 agents, no errors**: **20 raised → 1 confirmed** (ARCHITECTURE.md still described the seven-package layout). Also added `@jobportal/domain` to `transpilePackages` — the review refuted that one **3/3 five times** and was right that it works today, but `next.config.ts` states the rule in its own comment and domain was the only one of eight deps missing. Gate green on the integrated state: typecheck **13/13** (12→13 workspaces) · tests **10/10** · build **5/5**. **On pull run `pnpm install`** (new workspace package). |
| 2026-08-08 | `feature/api-contract-and-mobile-auth` | **Mobile API foundation — the app can finally log in** (ADR 0002 decisions 1–2; `apps/api` only — `apps/web`, `apps/recruiter`, `apps/sadmin`, `apps/services` and every `packages/*` verified byte-untouched by an empty `git diff`; **no schema, no migration, no new flag key, no shared-surface lock**). **Nothing to run on pull.** The CQ mobile team's spec asked for 9 public endpoints and never mentioned the one thing blocking all of them: **`JwtAuthGuard` already accepted `Authorization: Bearer`, but nothing ever handed a phone a token** — register/login/refresh/reset all emitted tokens only via `Set-Cookie` and returned `{user}`, and `/auth/refresh` read the refresh token *exclusively* from the cookie. The app could present a credential it had no way to acquire. Three things ship. **(a) URI versioning** with `defaultVersion: VERSION_NEUTRAL` — the load-bearing choice: every existing controller keeps its exact path and only an opted-in controller moves to `/v1`, where a plain `setGlobalPrefix('v1')` would have relocated all 36 controllers and broken three shipped web apps in one commit (verified live: `/auth/login`, `/me/profile`, `/health`, `/auth/google/status` all answer on their old paths; `/v1/auth/login` correctly 404s). **(b) One documented error envelope** — codifies Nest's OWN `{statusCode, error, message}` rather than inventing one, because `HttpException.createBody` already produced it for both the ~121 Zod `throw new BadRequestException(issues)` sites and the plain-string throws, so no existing web caller changes; the Zod issue ARRAY is deliberately preserved, since flattening it would break field-level form validation in all three apps. **(c) `/v1/auth/mobile/{register,login,refresh,logout}`** returning tokens in the body — a SEPARATE controller, not a client-sniffing branch on `/auth/*`, so the three web apps cannot break; every token decision still lives in `AuthService`. A **deliberate, owner-approved divergence from CLAUDE.md §9** (HttpOnly cookies): a native client has no cookie jar. `expiresIn` is a DURATION, never an absolute timestamp — a wrong device clock would otherwise refresh on every call or never. **Adversarial 5-lens review, run TWICE**: the first run had **7 dead agents including the entire route-drift lens**, so it was re-run rather than reported as a pass (route drift came back clean on the re-run). Across both: **51 raised → 9 confirmed (5 distinct), all fixed** — a **HIGH** where the filter rewrite left the pre-existing `sentry.filter.test.ts` **red** (`host.getType()` on its `{} as ArgumentsHost`), caught because the earlier “25/25 pass” covered only the two new files; a **MED** where the new `Retry-After` **overwrote ThrottlerGuard's accurate `timeToBlockExpire` with a flat 60**, making the very mobile back-off signal this branch advertises *worse* than develop (now fills gaps only — measured decaying 47→41→35); a **MED** where writing the response ourselves dropped Nest's `ExceptionsHandler` log, so with `SENTRY_DSN` blank a 500 became **completely silent** and the telemetry killswitch would also have blinded the logs; a **MED** where `retryAfterSeconds` guessed 60s for a **1-hour** per-email lockout and a **monthly** recruiter quota (now answers only when the body says something definite, and `PerEmailThrottleGuard` reports its own Redis TTL); and a **MED** where body-parser http-errors (413 `entity.too.large`, 400 `request.aborted`) **collapsed to 500** and were reported to Sentry as 5xx (verified live: a 200KB POST now returns a real `413`). **API 812 → 858 tests** (+46). Gate green on the integrated state: typecheck **12/12** · tests **9/9** · build **5/5**. |
| 2026-08-03 | `feature/sadmin-candidate-management` | **sadmin "Candidate Management" — the candidate master list, and a new rail item below Employer management** (`apps/sadmin` only; `apps/api`, `apps/web`, `apps/recruiter`, `apps/services` and every `packages/*` byte-untouched). **No schema, no migration, no new flag key, no `theme.css` token, no barrel edit, no lock taken.** **Nothing to run on pull.** Columns: name + avatar · headline · email + phone · location · registered · Actions. **View / Suspend / Delete render INERT** — scoped to a later PR; Suspend additionally cannot be wired until a `User.deactivatedAt` migration exists (there is no suspension column on `User` at all today, only `Recruiter.deactivatedAt`). **The list queries `User where role=CANDIDATE`, NOT `prisma.candidate`** — the `Candidate` row is provisioned lazily on first `/profile` read, email+password signup creates only the `User`, and the Google path swallows a failed `Candidate` create, so driving off `Candidate` would silently omit real registered seekers. Search (name + email, owner-chosen scope) is a debounced client island: a plain GET `<form>` is NOT a safe simplification because basePath `/sadmin` is applied by the router but not to a raw form `action`. The where-clause is built once and passed to both the `findMany` and the `count`, so the total, the count line, the pagination and the over-range redirect cannot disagree. **Adversarial 5-lens review, 3 skeptics per finding: 11 raised → 6 confirmed (4 distinct), 5 refuted — all fixed.** Headline **HIGH: search results changed SILENTLY for screen readers** — the island was copied from recruiter `JobsFilterBar` but not the `<p role="status">` that page pairs with it; since the bar commits with `router.replace(..., {scroll:false})`, focus never moves and the title never changes, so Next's route announcer says nothing either, and narrowing 1,240 rows to 0 was announced by nothing at all. **MED: `?q=a&q=b` 500-ed the route** (Next delivers `string | string[]`; `raw.trim()` on an array — reproduced in the dev server) → `firstParam`, the guard recruiter already uses. **LOW: the avatar monogram was announced before every name** ("P S Priya Sharma") → `aria-hidden` on the root. A further defect was caught by browser verification BEFORE the review: `title` won the accessible-name computation over the button content, so all sixty action controls were named "Not available yet" → explicit `aria-label`. Gate green on the integrated state (**typecheck 12/12 · tests 9/9 — 1,447 total, sadmin 175 → 216 · build 5/5**); `/candidates` builds `ƒ` Dynamic, matching `/employers`. **Browser-verified**: 20 rows, monograms, em-dashes for absent phone/location; search filters and keeps focus + caret; empty state says "No candidates match" not "none registered"; `?page=99` and `?q=a&page=99` both return a real server **307** (never a soft 200) preserving `q`; pagination proved on 25 temporary rows — 20 + 5, **zero overlap across the seam**, which also proves the `id` tiebreak since those rows share a `createdAt`; the `role="status"` region is the SAME DOM node before and after a result change (a remounted one would not announce); 12/12 contrast samples pass (min 7.14, canvas-composited, sanity-checked at 21:1); no overflow at 1280 or 375; 0 console errors. **The 25 probe rows were deleted and the DB verified back to its exact baseline.** |
| 2026-08-03 | `bugfix/seed-sequence-monotonic` | **The demo seeds could rewind a sequence below live rows — that is why signup was 500ing with P2002 on `User_pkey`.** Both seeds closed with an unconditional `setval(seq, <constant>, true)`, so re-running one on a database that had grown real rows above that constant pushed the sequence back under them. Both sites now call **`advanceSequence()`** (`packages/db/src/sequence.ts`) = `GREATEST(max(id), current value, floor)`, which can only move forward. **`packages/db` only — no schema, no migration, no flag, no lock; nothing to run on pull.** If your local signup is broken, `pnpm db:seed:demo:full` now repairs the sequence instead of breaking it (or just `pnpm --filter @jobportal/db db:seed:demo:apps`). ⚠️ Adds a `test` script to `packages/db` — `pnpm test` now fans out to **9 workspaces** once integrated with the nav loader (which added the other new one), and 7 of the new tests need a reachable local Postgres; **they skip with a reason if Docker is down**, so the gate stays green either way. |
| 2026-07-30 | `feature/brand-nav-loader` | **Global navigation loading feedback — the “The Advance” brand loader, live in all three portals** (`packages/ui` + `apps/web` + `apps/recruiter` + `apps/sadmin`; **no schema, no migration, no new flag key**; `apps/api`, `apps/services`, `packages/types`, `keys.ts` byte-untouched). **Nothing to run on pull.** Tester: “user is unable to understand the pages are opening or not” — measured on the dev server the tester uses, `/jobs` took **1.9–6.8s** and `/career-advice` **7s** while the router held the OLD page frozen, and `apps/web` had **zero** `loading.tsx` and no navigation indicator anywhere. **`loading.tsx` was deliberately NOT the mechanism**: this repo already measured a route-level fallback degrading `notFound()` 404→200 and an over-range `redirect()` 307→200 (employer-management PR), which on `/job/[slug]` and `/company/[handle]` would break the SEO status codes — so the fix is one client-side controller on Next 16.2's own APIs, **no new dependency**. **Design** came from a judge-panel Workflow (4 concepts → 3 judges → design lead; **“The Advance” was the unanimous #1 at 51/53/49**) pitched as a full-fidelity interactive **Artifact** with live 1.5s/4s/7s/reduced-motion scenarios, and the owner picked it over the staged “Now Serving” alternative (which would have needed a prerequisite PNG→SVG logo migration across three shells plus sign-off on rearranging the lockup). **What it is:** past **250ms** of genuine waiting a flat white veil rises and the mark performs the brand's own promise — the cyan arrow launches from the C's seam, docks on the Q, and for **450ms of total stillness the composition is the EXACT static lockup**, reassembled every 1.8s at a **constant tempo** (a queue advancing at a steady rate is the trust signal, so nothing ever escalates; the only evolution is one quiet “Still loading” caption at 3s). **The mark is vector-traced, not drawn** — every coordinate comes from row/column pixel scans of the shipped `cq-mark-color.png`, so the C's diagonal terminal falls out of a butt-capped 42° arc and the arrow paints over the C's lower stroke exactly as the brand file composites it. **The arrow's resting coordinates ARE the docked lockup**, which is what makes reduced motion correct for free: `theme.css`'s global kill rule collapses the loop to the finished mark, and a media query swaps the 3s caption for an immediate “Loading” so text carries the signal when nothing moves. Recruiter/sadmin get a **100% opaque pane-only veil offset `md:left-[256px]`** (browser-measured left:256 width:1024 at 1280px) because a translucent sheet ghosting the navy rail reads as a compositing bug, not a loading state. Timing lives in a **framework-free machine** (`packages/ui/src/lib/nav-progress.ts`): 250ms show delay · 400ms min-visible floor · 170ms CSS exit · 15s failsafe · re-navigation **reuses the running loop** (a restarting arrow would read as the queue resetting). Detection is a document **bubble-phase** click listener that **deliberately ignores `defaultPrevented`** — Next's `<Link>` preventDefaults every client nav, so “prevented” is what a real navigation looks like, while in-link hijackers (`JobCardSaveToggle`) `stopPropagation` and never reach us. Each app wrapper also patches `router.push/replace` so SRP filters, sorts, header search and pagination signal too; `refresh()` is **deliberately unpatched** (it never changes the route key). **First vitest harness in `packages/ui`** → `pnpm test` now runs **8** workspaces. **Adversarial 5-lens review with a per-finding refuter: 25 raised → 23 confirmed (10 distinct), all fixed** — headline **HIGH: a same-URL `router.push`** (a search re-submitted unchanged, a notification linking to the page already open) **stranded an input-blocking veil for 15s, and the natural retry gesture re-armed the failsafe for another 15s each time** → new `isSameDocumentNav` guards all three patches (canonicalized query compare so `%20` vs `+` can't defeat it, basePath-aware for sadmin, reads `window.location` at call time because the patch installs once). **MED Back/Forward across a hash-only history entry** (the real `#openings` anchors on company pages) fired a navStart no commit would ever answer → popstate now compares the post-traversal location against the last committed one. **MED a bfcache restore resurrected a timerless veil** after `pagehide`'s destroy → pagehide clears React state and `pageshow(persisted)` resets again. **Four findings collapsed into one decision: the veil is now `pointer-events-none` for its whole life** — pure feedback, zero input interference — which killed both the invisible 80ms blocking window after the fade and the WCAG 2.2 inequity of a sheet that blocked mice but not keyboards. Plus: the min-visible floor moved to `performance.now()` (a backwards NTP step held the veil for the step's duration), the live region now escalates to “Still loading” at 3s and says “taking longer than expected” on a failsafe instead of leaving “Loading page…” as the last thing heard, re-navigation during the exit no longer blinks through transparent, and the clip-path comment that was **geometrically false** now matches the real slant line. Gate green on the integrated state (**typecheck 12/12 · tests 8/8 — API 812 / web 216 / recruiter 83 / sadmin 175 / **ui 27 (new)** / auth 17 / feature-flags 37 / observability 19 · build 5/5**). **Browser-verified in all three portals**: veil shows and exits on real slow navs, `pointer-events:none` measured, keyframes bound, `role=status` announces, pane geometry measured, 0 console errors — and every fixed stranding scenario re-verified (same-query re-submit → **no veil ever**; `#openings` click then Back → **no veil ever**; a real cross-page nav still shows it). **Known gap:** recruiter/sadmin `/login` sit outside the authed shell and have no loader — static sub-250ms routes, below the show threshold. |
| 2026-07-30 | `bugfix/forgot-password-otp` | **RPT-008 — password reset reworked from an emailed LINK to a 6-digit OTP, and `/forgot-password` rebuilt** (`apps/api` + `apps/web` + one schema change; recruiter/sadmin/services untouched). Tester: no email arrives, and the flow sends a reset *link* where every other platform sends a code. **TWO SEPARATE CAUSES.** (1) **Nothing was ever delivered — configuration, not code**: `RESEND_API_KEY` is blank, so `ResendClient` logs a stub and returns, silently dropping EVERY transactional email (reset, verification, invites, alerts). Proven from the API log. Owner's call: defer the key, add a **strictly dev-gated** fallback (`NODE_ENV !== production` AND no key) that logs the code so testers are unblocked — dead code in production. **A real Resend key is still required before launch.** (2) The flow was link-based by design → reworked to **email → 6-digit code → verify → new password → auto sign-in** (owner-approved). **Schema (LOCK TAKEN + RELEASED, NOT purely additive):** `PasswordResetToken` gains `codeHash`/`attempts`/`resendCount`/`lastSentAt`/`verifiedAt`, `tokenHash` becomes the post-verification ticket (**NOT NULL dropped**), `@@index([userId])` **swapped for** `@@unique([userId])` so a resend replaces the code in place, and the migration **DELETEs every existing row** (a link token has no code to verify and `codeHash` is NOT NULL — outstanding reset links stop working by design). **Security** (six digits is a 10^6 space): the attempt cap is claimed by a conditional UPDATE *before* the comparison so it fails CLOSED under concurrency; codes hashed at rest salted with `userId`; constant-time compare; the DTO rejects non-6-digit input so junk cannot burn a victim's attempts; **no enumeration** — unknown addresses, OAuth-only accounts, cooldowns and spent budgets all return the same shape and never throw; every other session revoked in the same transaction. **UI: "The Reset Ledger"** — chosen by the owner from a judge-panel design workflow (4 concepts → 3 judges → 2 shortlisted) presented as a full-fidelity interactive Artifact. Three steps on screen from first paint, finished ones collapsing to a receipt row carrying the fact they proved (`Email · p•••••h@gmail.com`, `Code · Verified`); **no Back button anywhere**, because `Change` lives on the collapsed email row — which is also the only exit from a real trap (a typo'd address gets the generic error forever). OTP field is **one real input under six painted wells** (autofill/paste/AT/native editing all target one control; paste prefers the LAST run of six digits so "Career Queue 2026 — your code is 483920" cannot yield "202648"). New `ResetLedger`/`LedgerStep`/`OtpField`/`FormError` (ported from recruiter)/`lib/auth/password-rules`; `PasswordInput` gains OPTIONAL controlled visibility so `LoginForm`/`RegisterForm` are untouched; `/reset-password` now redirects. **No `packages/ui` or `theme.css` edit.** **Adversarial 4-lens review: the first run reported 0 findings but all four agents had died on API 529s — an infrastructure failure, not a clean result; re-run properly → 28 raised, 21 confirmed (13 distinct), ALL FIXED.** Headline **HIGH: a soft-deactivated recruiter could regain a session** — `login()` blocked them but the check lived in `login()`, while `issueSession` is shared by login, Google OAuth and now the reset; both of the latter skipped it, and the recruiter jobs/applicants controllers authorise on the JWT role claim without re-checking `deactivatedAt`. Moved into `issueSession`, closing the Google path that predates this branch. **MED `resetWithTicket` did not fail closed** — Prisma's array-form `$transaction` commits every statement unless one rejects, and `updateMany` matching zero rows resolves, so the guard only shaped the HTTP response while the password write had already committed (the loser of a double-submit changed the password and signed the winner out while being told it failed) → interactive transaction. **MED the code was not single-use** (re-verifiable, minting fresh tickets and voiding the held one, redeemable past its 15-minute life) → gated on `verifiedAt` + code burned. **MED the tests could not have caught either** (`$transaction` was a bare `vi.fn()` that never ran the callback) → the mock now models real semantics and asserts nothing is written on the losing path. Plus: the send gate was a read-then-write race (→ advisory lock, the `RecruiterOtpService` device), `stale` ignored `usedAt` (a completed reset blocked the next one), no focus management on step changes, errors never associated with their controls, one `busy` flag drove three requests (a resend claimed "Checking your code…"), countdowns compared server timestamps to the device clock (a fast clock declared a live code expired → API now returns durations and the client counts elapsed local time), `<div>` inside `<p>` in the email, and an `@@index` comment promising a purge sweep that does not exist. Gate green (typecheck 12/12, tests 7/7 — **API 812** / web 216 / recruiter 32 / sadmin 86 / auth 17 / feature-flags 37 / observability 19, build 5/5). **Browser + API verified end-to-end**: happy path → auto sign-in landing on `/profile`; wrong code "4 attempts left" with red wells but DARK digits; unknown address gets the generic message; malformed code rejected WITHOUT spending an attempt; code single-use; ticket replay rejected; a new request right after a completed reset works; countdown decrements locally (14:58→14:56); focus moves into each step; all `aria-describedby` ids resolve; 0 contrast failures; no overflow at 1280/375; 44px tap targets; 0 console errors. **DB restored byte-identically** after every run (3 users' password hashes saved + compared, reset rows cleared). **FOLLOW-UPS:** (1) ⚠ **provision `RESEND_API_KEY`** — no email of any kind is delivered until then; (2) a purge sweep for expired reset rows. |
| 2026-07-29 | `feature/recruiter-signup-otp-verification` | **Recruiter signup now requires OTP-verified email + mobile**, and a new sadmin **OTP Sessions** relay console. ⚠️ **On pull run `pnpm db:generate && pnpm db:migrate:dev`** — 2 migrations. ⚠️ **Behaviour change:** a recruiter cannot create an account until both channels are verified; there is no SMS/email provider, so staff relay the code from `/sadmin/otp-sessions`. Registration no longer sends the work-email link (the OTP proves the same address) and sets `workEmailVerified` true. Also wires `killswitch.new_registrations`, which had zero enforcement anywhere until now. Reused `User.phone` rather than adding a column; `packages/ui`/`theme.css`/`packages/types`/`keys.ts` untouched. Gate green (typecheck 12/12 · tests 7/7 — API 791 / web 216 / recruiter 83 / sadmin 175 · build 5/5). |
| 2026-07-29 | `feature/recruiter-auth-ui-revamp` | **Recruiter sign-in / sign-up rebuilt as a two-pane split — brand-navy panel left, form right** (SRS §4.9). **`apps/recruiter` only** — `apps/api`/`apps/web`/`apps/services`/`apps/sadmin` and every `packages/*` verified byte-untouched by an empty `git diff`; **no schema, no migration, no new flag key, no shared-surface lock** (every icon was already in the barrel). **Nothing to run on pull.** The panel is the SAME fixed navy as the authed sidebar rail, so signing in previews the shell you land in — which forces two non-cosmetic details: the logo must pass `onDark` (this app mounts no ThemeProvider, so the auto path would put the navy logo on navy) and the focus ring must be white (the inherited `--color-ring` is **1.96:1** on navy, under the 3:1 floor; measured **15.37:1** after). **Illustrations are flat brand geometry built from the product's own primitives** — a job card, an applicant list with one row shortlisted, a compose card with Publish beside Save-as-draft — not stock art or a mascot, per CLAUDE.md §2 and the `ArticleCover` precedent. The panel headline is a `<p>`, not a heading, so each page keeps exactly one `<h1>` and there is no h2-before-h1 in DOM order. **The shell is owned by each PAGE, not the (auth) layout** (that file is deleted): the layout version resolved its copy from the request pathname and was broken — the App Router does not re-render a shared layout when navigating between two of its children, which is exactly the `/login` ↔ `/register` links. Forms moved into `LoginForm`/`RegisterForm` client islands (handlers verbatim) so the pages are server components and the panel never enters the client bundle. **Zero functional change**: same endpoints, bodies, `credentials`, `safeNext`/`?next=`, redirects, field names, `autoComplete`, `maxLength`, `role="alert"`. a11y gains: helper text bound via `aria-describedby`, ids via `useId()`, `fg-subtle` hints → `fg-muted`, error text off raw `--color-danger` (4.0:1) onto the repo's theme-aware `color-mix` recipe (**6.43:1**), 44px submit target. **Adversarial 5-lens review, 3 skeptics per finding: 34 raised → 5 confirmed across 3 distinct defects, 29 refuted** — HIGH the stale panel on soft navigation (found twice); MED a `headers()` call that forced all four routes dynamic behind a comment that was wrong on both counts (**/login + /register are back to `○ Static`, matching develop's route table**); MED the dashed "reach" curve ran through the Save-as-draft pill (11 of 21 sampled points inside it) → removed, the cards are 12 units apart; MED the decorative aside forced page scroll on a real 1366×768 laptop (~650px viewport, **24px over**) → vertical rhythm now clamps against viewport height, **0 overflow at 650px and 600px**. Gate green (**typecheck 12/12 · tests 7/7 — recruiter 32 → 53 · build 5/5**). **Browser-verified**: sign-in → `/dashboard`, wrong password → the alert with submit re-enabled, `?next=%2Fjobs%3Fstatus%3DACTIVE` → lands on `/jobs?status=ACTIVE`, register duplicate-email → error with all 4 values preserved; soft-nav panel swap proved **both directions with a window canary**; **0 contrast failures** on both pages (min 5.21, canvas-composited, sanity-checked at 21:1); no horizontal overflow at 1440/1366/1280/1024/1023/768/375; 0 console errors on a clean tab. **Not verified: the register happy path** — a real `RESEND_API_KEY` is configured, so creating an account would write User+Recruiter+Company and attempt an outbound email. |
| 2026-07-29 | `feature/sadmin-employer-management` | **sadmin "Employer Management"** — a third nav item, **`/employers`** (master list) + **`/employers/[id]`** (employer profile). **`apps/sadmin` only** — `apps/api`/`apps/web`/`apps/recruiter`/`apps/services`/`packages/*` verified byte-untouched by an empty `git diff`; **no schema change, no migration, no new flag key, no shared-surface lock held** (`Building2` was already in the icon barrel). **Nothing to run on pull.** **One row per `Company`, not per recruiter** (owner's call): several recruiters share one company, so four of the six requested columns are *derived*. `resolveContact()` takes the active OWNER → else the longest-standing active recruiter of a lesser role → else none, **always rendering the role beside the name** so a stand-in Member cannot read as the owner; a deactivated recruiter is never offered (sessions revoked, login blocked — they cannot be reached). Precedence is an explicit `Record<RecruiterRole, number>`, not `orderBy: { companyRole: 'asc' }`, which yields OWNER first only by accident of the enum's declaration order. **Account status = Active / Deactivated / No account holder + verification**; the third state matters because **4 of 14 companies have zero recruiters** and hold **live jobs nobody manages** — em-dashes alone would have read as an ordinary row. Registration date is **`Company.createdAt`** (self-registration into an existing company is rejected, so a later recruiter's date means "this person joined"); verification is `kyc?.status ?? 'NOT_SUBMITTED'` (**13 of 14 have no row**, and the schema says absence *is* that state); GSTIN/PAN deliberately not selected (DPDP). Job counts come from a `groupBy` with **every bucket named** so no total folds in DRAFT. **Reads Postgres directly in the RSC — no API module** (reversed from this branch's own opening claim: the API path is for logic that must agree with a mutation endpoint; this is display-only). **A `loading.tsx` was written, measured to silently degrade `notFound()` 404→200 and the over-range `redirect()` 307→200, and deleted** — which also reclassifies the `/jobs` "missing loading.tsx" follow-up as a **constraint, not an oversight**. **Adversarial 5-lens review, 3 skeptics per finding: 30 raised → 5 confirmed, 25 refuted** — an id above int32 answering **500 not 404** (first sadmin route reading Prisma directly, so `adminApiGet` was not there to absorb the throw; hex/exponent ids also resolved real rows), the **Deactivated/Removed labels failing AA** (4.41:1 card / 4.02:1 hover → theme-aware `color-mix`, 7.03:1 / 6.45:1), two comments citing a seed transaction **that does not exist**, and an unfocusable scroll region hiding the columns that explain a Deactivated employer. Gate green (**typecheck 12/12 · tests 7/7 — sadmin 86 → 131 · build 5/5**). **Verified against SQL + a running server**: all 14 rows value-for-value; Acme **Live 3 / Drafts 1 / All postings 4**; the stand-in, Deactivated and pagination paths **forced into existence** (20+2 rows, 22 unique ids, zero seam overlap, `?page=99` → **307 → `?page=2`**) then **DB restored byte-exact**. |
| 2026-07-29 | `feature/navbar-mega-menu-console` | **Seeker navbar mega-menu redesigned as "The Console"** (`apps/web` only; `packages/*`/other apps/`theme.css`/the icon barrel byte-untouched; **no schema/migration/flag/shared-surface lock**). The owner rejected the shipped **"Native tiles"** look (and, before it, two editorial-column attempts) and asked for "pure modern, unique, professional". A judge-panel design Workflow produced 4 fresh silhouettes, 3 judges ranked them, and a design lead shortlisted 3 non-overlapping directions — **Console / Bento / Marquee** — which were pitched as ONE interactive full-fidelity **Artifact** on the live tokens and the real seed data. The owner picked **The Console**. **What it is:** a two-pane master/detail popover — a 216px facet rail (a real ARIA `tablist`) beside a detail pane, stitched by **ONE 3px cyan indicator that glides** between facets (the "one thing moving" discipline) — over a full-width footer with the live count, the honest quick-views and the single filled-navy CTA. **Three craft moves:** the detail holds a fixed `min-h-[15rem]` so the popover **never resizes on swap** (browser-measured: 298px constant across all four facets); each Jobs row carries an **honest proportion bar** (fill = that row's share of the facet's own max, 8% floor, cyan-capped leader) — *never animated*, since animating a data length would misrepresent it; and the whole panel keeps ONE elevation + a strict cyan-as-marks-only budget. **Jobs** = Roles/Locations/Industries/Skills (facet dropped below 2 items); **Companies** = Collections/Industries/Featured, with industry rows deliberately **count-free** (the only number held is a JOB count, which would misread as a company count). New client island **`FacetTabs.tsx`** takes the rail icons, every pane and the footer as **SERVER-rendered ReactNode slots** (slot pattern one level deeper), so no presentational code enters the client bundle and all links stay in the crawlable HTML; inactive panes use the `hidden` attribute so they leave the tab order + a11y tree. Retired `NavTile.tsx` + `menu-chrome.tsx`; `PrimaryNav`'s open/close contract, `lib/nav/menu-data.ts` and `nav-hrefs.ts` are **unchanged**. Only `globals.css` gained one app-owned `pane-swap` keyframe. **Adversarial 4-lens review (correctness-RSC / a11y / brand-craft / isolation-perf, each finding independently refuted): 19 raised → 13 confirmed (8 distinct), all fixed** — headline **MED: clicking a facet focused the button, which satisfied `PrimaryNav`'s "focus is inside the group" guard and defeated the mouse-leave close, pinning an 834px panel to the sticky header until Escape** (tab now `preventDefault`s mousedown, so only keyboard selection moves focus); **MED a stray pointer over the rail while keyboard focus sat on a link in the open pane hid the focused element, dropping focus to `<body>` and closing the popover** (pointer selection now bails while focus is in the pane); LOW the indicator was a hardcoded **38px** against rem-sized rows so it drifted onto the wrong row at any non-16px root font → `calc(i * 2.375rem)`; LOW `active` was never clamped to a shrinking data-derived tab set; LOW **"Top rated" and "All companies" both resolved to `/companies`** (rating IS the default sort) → A–Z now uses `?sort=name`, **+2 regression tests**; LOW the pane reused `.rise`, a 250ms translateY *entrance* keyframe, so sweeping the rail shifted and blanked it → a dedicated opacity-only `pane-swap`; LOW the empty-DB fallback was too narrow for its footer → reduced strip; NIT a `max-w` clamp that could not bound the right edge (the panel is positioned from the trigger) → dropped rather than left as decoration. Gate green (typecheck **12/12**, tests **7/7** — API 683 / web **216** / recruiter 32 / sadmin 86 / auth 17 / feature-flags 37 / observability 19, build **5/5**). **Browser-verified** (real data, JS probes): tick lands exactly on rows 0/38/76/114 and matches the measured row offset; panel height constant across facets; hover===focus===select plus roving tabindex/Arrow/Home/End; the two MED fixes re-verified (mousedown default prevented; stray hover holds the pane and focus survives while keyboard selection still switches); 4 distinct Collections URLs; **113 text nodes, 0 contrast failures** (labels 17.92:1, active tab 15.56:1, footer figures 15.37:1, muted 7.79:1); no horizontal overflow at 1360 **or** 1024; closed panels keep 27 links in the DOM but `inert`; open/close contract intact; 0 console errors. |
| 2026-07-29 | `feature/job-moderation-queue-ui` | **Job moderation — PR 2 of 3: the `/sadmin` review queue, and the flag turned ON** (SRS §4.9/§7). **⚠️ Behaviour change for everyone: after `pnpm db:migrate:dev`, publishing a job no longer makes it live — it lands in "Under review" and needs approving at `/sadmin/jobs`** (sign in as the seeded `admin@careerqueue.in`). One toggle in `/admin/feature-flags` turns it back off; pending jobs stay actionable either way. **New in `apps/sadmin`**: `/jobs` (FIFO queue on `submittedForReviewAt`, with the moderation-off state stated so an empty list can't read as "all caught up") and `/jobs/[id]` (whole posting + the employer's verification status + Approve / Send back; recruiter text rendered as **plain text, never markup** — it is untrusted content shown to staff to judge). **Two views, not a status filter**: `ACTIVE` is every live job on the platform, so an "approved" tab would have listed 52 jobs nobody reviewed — `decided` is `reviewedAt IS NOT NULL`, which is also the **only readable record of a moderation decision anywhere** (`ProfileAuditLog` has no reader). First **mutation** surface in this app → new `lib/admin-api.ts` (forwards the HttpOnly cookie to the AdminGuard'd endpoints); the dashboard still reads Postgres directly, the queue goes via the API so it can't fork logic the decision endpoint enforces. **The flag flip needed TWO mechanisms**: `seedFlags` upserts with `update: {}`, so the new seed default reaches fresh DBs only — migration `enable_job_moderation_flag` moves the rest. **Recruiter-facing**: badge → **"Under review"**; publishing now confirms the outcome via `?submitted=` taken from the **API's returned status** (it used to redirect silently, which reads as failure); Jobs-by-status always shows the review row; edit-page copy is status-aware; demo seed stops re-asserting `status` (it had been silently emptying the queue on every reseed). **Adversarial 5-lens review: 35 raised → 10 confirmed (7 distinct), all fixed** — Reopen dialog still promised "goes live again immediately"; three FAQ answers still described the pre-moderation model (including a status glossary missing the new state); a salary formatter printed a **ceiling as a floor** (`₹12+ LPA` for a job capped at ₹12) **with a unit test asserting the wrong string**; `EMPLOYMENT_LABEL` invented two enum values and omitted the real `CONTRACTOR` (both label tables are now keyed by the Prisma enums, so drift is a **compile error**); an over-range `?page` rendered a false, dead-end empty state; +3 stale comments. Gate green (**typecheck 12/12 · tests 7/7 — API 683 / web 214 / recruiter 32 / sadmin 60 → 86 · build 5/5**). **Browser-verified against a real posting**: anon 404 + absent from ES + owner 200 → queue → send-back (validated, reason stored, notification + audit) → republish (stale reason cleared) → **approve → ACTIVE, in ES (200), publicly visible (200)**. **DB + ES restored byte-exact.** **On pull run `pnpm db:generate && pnpm db:migrate:dev`.** |
| 2026-07-29 | `feature/job-moderation-api` | **Job moderation — PR 1 of 3: admin approve/reject for jobs awaiting review** (SRS §4.9/§7). **Closes the `moderation.jobs.enabled` trapdoor** — `PENDING_MODERATION` had **no exit anywhere in the repo** (close/reopen 400 on it, publish accepts DRAFT only, PATCH never writes status, the nightly sweep selects ACTIVE only), so turning the flag on stranded every subsequent posting with DELETE as the only recovery. **Schema** (2 migrations, lock held): `add_job_moderation` (`Job.submittedForReviewAt`/`reviewedAt`/`reviewedById`/`rejectionReason` + `NotificationType`/`ProfileAuditAction` `JOB_APPROVED`/`JOB_REJECTED`) and `add_job_review_queue_index_and_quota_flag` (`Job.postQuotaConsumed` + `@@index([status, submittedForReviewAt])`). **No new `JobStatus`** — reject returns the job to DRAFT + reason, so the five hand-maintained copies of the status set stay untouched. **New `apps/api/src/admin-jobs`**: `GET /admin/jobs` · `GET /admin/jobs/:id` · `PATCH /admin/jobs/:id`, AdminGuard'd, **not** gated by the moderation flag (turning intake off must not strand an existing queue — the admin-kyc/admin-support rule); the decision is an **atomic status-guarded `updateMany` committed in one transaction with its `ProfileAuditLog` row**, so concurrent Approves can't double-index/double-notify (loser → 409). **Extracts `JobPublishEffectsService`** so approval replays the exact ES+alerts+purge+email sequence and re-clears a stale `expiresAt` (a job can outlive its expiry while pending, where the ACTIVE-only sweep can't see it, and would go live only to be re-expired within 24h). **Fixes two real holes**: `reopen()` set ACTIVE unconditionally without reading the flag (**Close → Reopen was a two-click bypass**; and since PATCH may rewrite a CLOSED job's text, Close → Edit → Reopen laundered unreviewed content), and the **public `/job/[slug]` served DRAFT/PENDING_MODERATION jobs to anyone with the URL** — full description, salary, `JobPosting` JSON-LD — with the canonical 308 leaking the title in a `Location` header *before* any status check. New `lib/job/visibility.ts` 404s non-public jobs for all but the poster, collaborators and admins (recruiter "Preview" still works). Also: `reindex.ts` now indexes ACTIVE only (a full rebuild had been writing DRAFT/PENDING docs into the live alias); `quota.refund()` no longer leaves a negative no-TTL counter; the apply-block copy no longer leaks the raw enum; `ClosedJobNotice`'s undefined `--color-surface-subtle` fill fixed. **Flag stays OFF — PR 2 flips it.** **Adversarial 5-lens review: 33 raised → 7 confirmed (2 distinct), all fixed** — both introduced by this branch's own `reopen()` change: a **free post slot** (reject refunded a slot reopen never consumed — farmable once per closed job; now gated on `postQuotaConsumed`) and **reopened jobs jumping the FIFO queue** (it sorted on `postedAt`, which reopen never refreshes; now `submittedForReviewAt` + its index). The review also caught a test that had **baked in the same false premise as the code**. Gate green (**typecheck 12/12 · tests 7/7 — API 637→680 / web 199→214 · build 5/5**). **Verified against a running stack**: anon 404 / owner 200 / no `Location` leak; 403+401 on the admin route; approve cleared a stale 2020 expiry and indexed to ES; **quota proved both ways — reopen→reject stays 3/3, publish→reject refunds 4→3**; queue order proved with a January-dated reopen ranking behind a 2-day-old submission; reindex 52 with the DRAFT 404 in ES. **DB + ES restored byte-exact.** **On pull run `pnpm db:generate && pnpm db:migrate:dev`.** |
| 2026-07-29 | `feature/navbar-mega-menu` | **Seeker navbar → hover MEGA-MENU for Jobs + Companies** (`apps/web` only — shared `SiteHeader` + new `components/nav/` + `lib/nav/`; **no schema/migration/flag/theme-token/shared-surface lock**; `theme.css`/other apps/packages byte-untouched). Hover/focus the **Jobs** or **Companies** nav link → a smooth brand-flat **browse-grid tile** panel opens (the "Native tiles" concept — chosen by a judge-panel design workflow, owner-approved via a full-fidelity Artifact; distinct from Naukri's cramped 3-column dump); **a plain click still navigates** to `/jobs` / `/companies`. **100% real data** via a new 30-min-cached **`loadNavMenuData()`** (a date-free subset of `loadHomePageData`): roles→`/jobs?q=`, cities→`?city=`, industries→`?industry=`, skills→`?skill=` (each with a live count); companies→industry collections `/companies?category=`, `?hiring=1`, `?sort=reviews`, top-rated employers→`/company/<slug>-overview-<id>` (reuses `CompanyLogo`). Links built via `buildSrpHref`/`buildDirectoryQuery` (canonical, no 301). Reuses the home browse-grid tile DNA (navy `primary-100` chip + label + count + cyan hover chevron). Server-rendered into the HTML (crawlable); closed panels `inert`; **slot pattern** keeps the presentational panels out of the client bundle. Monochrome-navy rating star + honest quick-views (owner calls). Deliberately NOT using `mode`/`emp` filters (no-op at the search layer) nor interview-questions/salary tools (no such feature). Desktop-only (`lg+`); mobile drawer untouched. Fixed panel widths (Jobs 51rem / Companies 36rem) — **no overflow at 1024/1360**. **Adversarial 4-lens review (correctness-rsc/a11y/brand-contrast/isolation, each refuted): 12 raised → 11 confirmed, all fixed** — Escape-from-inside-panel reopen guard, the documented-but-missing outside-pointerdown close, hover-close no longer drops keyboard focus, Escape restores focus only when it was inside, dropped `aria-haspopup` (region disclosure, not a menu), `sr-only` "Rating" label, `React cache()` on `loadHomePageData` (home cold-render double-fetch). +11 web tests (nav-hrefs → **web 199**). Gate green (typecheck 12/12, tests 7/7, build 5/5 on the integrated state incl. the `apps/sadmin` merges). **Browser-verified** (real seed): 27 Jobs / 15 Companies links resolve canonically; 4 facets 4-across; no overflow at 1360/1024; all text AA+ (labels 17.9, muted 7.79, navy count 15.4, navy star 7.83); `inert` on closed; focus-open + single-open + outside-pointerdown-close + Escape-close all verified; 0 console errors. |
| 2026-07-29 | `feature/sadmin-dashboard-insights` | **sadmin Dashboard — pending approvals · new signups · activity trend charts** (`apps/sadmin` only; `apps/web`/`apps/recruiter`/`apps/services`/`apps/api`/`packages/*` **byte-untouched**; **no schema change, no migration, no new flag KEY**, no shared-surface lock). **Pending approvals**: `CompanyKyc` PENDING (deep-links `?status=PENDING` into the existing admin queue so the list matches the count) + `Job` PENDING_MODERATION, with the **moderation flag's state stated** — nothing can *enter* PENDING_MODERATION while `moderation.jobs.enabled` is off, so a bare 0 would read "all caught up" when the truth is "this queue is not in use". Flag read via `@jobportal/feature-flags` per CLAUDE.md §4 (new dependency for this app), never an inline row read. **New signups**: today/7d/30d + candidate-vs-recruiter split + 30-day daily bars; ADMIN excluded (staff logins aren't signups). **Platform activity**: jobs posted + applications over 30 days as a 2-series line chart on one shared Y scale. **Charts are hand-rolled SVG — no chart library** (owner-confirmed); geometry lives in `lib/dashboard/chart.ts`, pure and unit-tested for what actually breaks charts (zero axis max → div-by-zero, single point → div-by-(n−1), sub-pixel bars rounding to nothing, degenerate boxes). Each chart renders an `aria-hidden` `<svg>` **plus a visually-hidden data table**, so it isn't an opaque image to a screen reader. **Day buckets are IST**, via `generate_series` + the **double** `col AT TIME ZONE 'UTC' AT TIME ZONE 'Asia/Kolkata'` — these columns are `timestamp WITHOUT time zone` holding UTC, and a single conversion is wrong in **both** directions (proved against this DB: 03:00 UTC → previous day; 19:00 UTC → fails to advance). India is UTC+5:30, so UTC bucketing would misfile the last 5h30m of every Indian day. Trends stream behind `<Suspense>`; counts + approvals are awaited. **Adversarial 5-lens review: 27 raised → 8 confirmed (6 distinct), all fixed** — headline **HIGH: "Jobs posted" counted DRAFT jobs behind a comment claiming the opposite** (`Job.postedAt` is `NOT NULL DEFAULT now()`, so drafts *do* get one; measured 53 reported vs 52 actually live — and the unit test had baked in the same false premise, so it asserted the bug rather than catching it) → now `NOT IN ('DRAFT','PENDING_MODERATION')`, reads **52**. Also: the KYC link landed on an unfiltered queue; `TREND_DAYS` labelled the charts but didn't drive the SQL window (hardcoded `INTERVAL '29 days'` ×3); the 2px line stroke was **clipped in half** at the viewBox edges; both activity series called `now()` independently (IST-midnight straddle could misalign the paired sr-only table) → one shared anchor instant; a comment cited a non-existent `KycStatus.APPROVED`. Plus a self-found perf fix: joining on a date **expression** can never use an index, so every load **seq-scanned the whole table** (EXPLAIN ANALYZE) → sargable range predicate in the ON clause (not WHERE, which would collapse the LEFT JOIN and destroy zero-filling): **4.0ms → 1.6ms** at 371 rows, and now independent of table size. Gate green (**typecheck 12/12 · tests 7/7 — sadmin 17 → 60 · build 5/5**). **Verified against SQL**: KYC pending 1, jobs-to-review 0, jobs posted 52, applications 371, axis 30 Jun→29 Jul; bar chart cross-checked with temporarily backdated users (30 bars, 5 non-zero, heights 45/45/90/45/45 against `niceMax(2)`) then **DB restored byte-identical**; 0 contrast failures (all ≥7.79:1); 0 console errors. |
| 2026-07-28 | `feature/sadmin-portal` | **NEW APP — the internal Super Admin portal (`apps/sadmin`, port 3003, `basePath: '/sadmin'`)**, the platform's third portal alongside the job-seeker site and the recruiter portal. Ships the **sign-in page** (email + password, no signup, show/hide eye toggle) and the first menu item, **Dashboard**, with three KPI cards. **No schema change, no migration, no new feature-flag key, no shared-surface lock** — `packages/ui`/`theme.css`/`packages/types`/`keys.ts` and `apps/web`/`apps/recruiter`/`apps/services` are all **byte-untouched** (verified by an empty `git diff develop`). **Reuses the existing `UserRole.ADMIN`** rather than adding a SUPER_ADMIN tier (that would need the schema lock, a migration, and an audit of every `requireAdmin()`/`AdminGuard` call site). **Design** is the recruiter shell verbatim: 256px brand-navy rail (**measured #192249**), white top bar, muted canvas, elevated white cards, cyan active-nav icon, white focus ring on the rail (**15.37:1**, vs the inherited ring's 1.96:1 on navy). **The `/sadmin` slug is `basePath` on ONE line of `next.config.ts`** and appears nowhere in app code, so moving to a subdomain later is a one-line deletion. Three basePath traps handled: `redirect()` must use `nextUrl.clone()`; the middleware matcher sees paths already stripped (**and needed `/` listed separately** — the catch-all never matches the bare root, which had silently left the entry URL without `X-Robots-Tag`); and **`next/image` does NOT prefix a string `src`** — the logo therefore uses static imports (whose `/_next/*` URL *is* prefixed) plus `unoptimized`, since the optimizer's own `url=` param is un-prefixed too. **Auth**: new **`POST /auth/admin/login`** — the existing `/auth/login` has NO role check, so it would hand a candidate a valid session on the admin origin. Role is checked **after** the password so it can't be used to discover which addresses are admins, with a byte-identical message (**verified: the same candidate credential returns 200 on `/auth/login` and 401 here**). Guard is `requireSuperAdmin()` (L2) — the role check is load-bearing because the cookie jar is shared across all portals on localhost. **KPI queries — both obvious versions are wrong**, in opposite directions: recruiters come from `Recruiter.deactivatedAt IS NULL` (removal is a soft delete that never touches `User.role`), seekers from `User.role='CANDIDATE'` (Candidate rows are provisioned lazily and under-count). Pinned by tests. **Seeded `admin@careerqueue.in` / `Admin@123`** via the *reference* seed (`pnpm db:seed`) so every dev gets it on pull; password re-applied on every run so all three machines converge, and existing sessions revoked. Guarded like the demo seed (**NODE_ENV *and* a DATABASE_URL locality test**) so the committed credential can't reach staging. **Adversarial 5-lens review: 31 raised → 20 confirmed (11 distinct), all fixed** — headline **two HIGH in the seed that would both have shipped broken**: `??` doesn't fall back on the **empty strings** `.env.example` ships, so the documented account would never have existed (a `role=ADMIN` row with an empty email/password would have, and the production guard would have compared `''` to the default and never fired); and reading env at **module level** ignored `.env` entirely, because `seed.ts` runs dotenv *between* its imports — a `SADMIN_SEED_PASSWORD` set in `.env` was silently discarded. Also: the upsert promoted a pre-existing row to ADMIN **without revoking its sessions** (registration is public and issues a 30-day refresh token whose claims are rebuilt from the live row); **Sentry was wired into the build but never initialised**, so every crash in the highest-privilege portal was dropped while appearing instrumented; the dashboard never set `data-wide`, rendering cards at 232px against the recruiter's 317px (**now measured 317px**); the loading skeleton's `role="status"` had no text content and announced nothing. Gate green (**typecheck 12/12 · tests 7/7 — API 637 (+8) / web 188 / recruiter 32 / sadmin 17 (new) / auth 17 / feature-flags 37 / observability 19 · build 5/5**). **Browser-verified**: KPI values **10 / 20 / 52 cross-checked against Postgres**; eye toggle flips `password`→`text` with `aria-pressed`; anon → `/sadmin/login?next=…`, a **signed-in recruiter → `/sadmin/login?denied=1`**, admin → 200; only the white logo renders on the navy rail; 0 console errors; no horizontal overflow at 1280/768/375. **Known limitation inherited from the recruiter shell: no mobile nav below 768px** (the rail is `hidden md:flex`), so the portal is desktop-only today. |
| 2026-07-26 | `feature/recruiter-brand-theme` | **Recruiter portal re-skinned onto the job-seeker dashboard's colour theme** (`apps/recruiter` **only** — `apps/web`/`apps/services`/`apps/api`/`packages/*` **byte-untouched**, verified by an empty `git diff develop`; **no schema/migration/flag/shared-surface lock**). Owner ask: match the colour system of the seeker profile/dashboard. Key finding: **both apps already import the same `packages/ui/src/styles/theme.css`**, so navy `--color-primary-600` (#192249) and cyan `--color-accent-500` (#22A0DA) were *already available* to the recruiter app — it simply never consumed them (**zero `--color-accent-*` occurrences** before this). That is why the whole re-skin needed **no change to `theme.css` or `packages/ui`**, which is what makes seeker-side regression impossible by construction. **Shell** (mirroring `DashboardChrome.tsx`): canvas → `bg-muted` so cards read as elevated; sidebar → solid navy, `border-r` dropped, 240→256px; top bar → white `bg-elevated`; nav rows → alpha-white (`white/70` idle, `bg-white/10`+white active); nav gains **18px icons with the ACTIVE icon in brand cyan**; three eyebrows (Hiring/Company/Account) that only **partition the existing order** — no destination moved/renamed/regrouped; account row gains a **cyan avatar + name + email + icon sign-out**. **Two changes required by the navy rail, not cosmetic:** `brand/Logo.tsx` gained `onDark` (the auto path keys off `[data-theme=dark]` and this app mounts **no ThemeProvider**, so it would have put the *navy* logo on the navy rail), and `SignOutButton` left the shared ghost `Button` (its `text-[var(--color-fg)]` is invisible on navy, and the atom is shared with `apps/web` so it could not be re-coloured). **Driven by the canvas move:** 21 bordered panels + 12 dashed empty states had **no fill** and would have rendered *darker* than the white cards beside them; 7 `bg-muted` fills sitting directly on the canvas would have gone invisible; on-canvas controls now hover to `bg-elevated`. The 4 near-black `bg-[var(--color-fg)]` selection states → brand navy. Also fixed `bg-[var(--color-surface-subtle)]` in `PostJobWizard`+`PostJobFlow` — **that token was never defined in `theme.css`**, so both panels rendered with no fill. **Deliberate deviation:** table row hover stays `bg-muted` rather than the seeker's `bg` — measured, `bg` is only a ~2% delta on a white card, too weak for 50-row tables (documented in-code). **Adversarial 4-lens review (behaviour/a11y/isolation/missed-surfaces, each refuted): 12 raised → 6 confirmed (5 distinct), all fixed** — headline: the navy rail **collapsed the keyboard focus ring to 1.96:1** (theme.css paints `:focus-visible` with `--color-ring` = `primary-500`, ~7.5:1 on the old white rail, under the 3:1 WCAG 1.4.11 floor on navy — and since theme.css sets an *author* outline the browser's adaptive ring is suppressed, so there was no fallback) → re-pointed to white, **measured 15.37:1**. Also: `Badge` `neutral` fills with `--color-bg-muted` = the new canvas, so **all 60 Post-a-Job skill chips went invisible** plus the neutral status pills in 4 page headers → new `NEUTRAL_ON_ANY_SURFACE` applied at call sites; the `/dashboard` `loading.tsx` skeleton opened with a **blank header** (Skeleton is also `bg-muted`); `FaqPanel`'s empty state was the one dashed panel the scripted sweep missed (it uses the shared `EmptyState`). Every fix is a **call-site `className` override** (tailwind-merge lets the caller win) — the technique `JobStatusBadge` already documents — so `packages/ui` stayed untouched. Gate green (typecheck 11/11, tests 6/6 — API 629 / web 188 / recruiter 32 / auth 17 / feature-flags 37 / observability 19 · **all baselines unchanged, as expected for a visual-only change** · build 4/4). **Browser-verified** (Priya @ Nimbus, real data): rail #192249 at 256px with no border, white top bar, canvas/card separation 11/10/8, **only the white logo asset renders**, cyan active icon + "PS" avatar, exactly one `aria-current` per page, 9/9 pages 200, 0 console errors, no overflow at 375. **Contrast sweep: 14 failures, all one PRE-EXISTING issue** (`fg-subtle` "Expires" line at 2.58:1, ~2.52:1 before this change) — **zero new failures**. **Follow-ups (both pre-existing, deliberately not touched):** ~37 `fg-subtle` uses portal-wide need a per-site AA pass; `/jobs` scrolls horizontally at exactly 768px on `develop` already (the table's `min-w-[880px]`) and the 256px rail adds 16px to it. |
| 2026-07-26 | `feature/recruiter-dashboard-insights` | **Recruiter Dashboard rebuilt — verification progress + KPIs/insights** (`/dashboard`, SRS §4.9; **`apps/recruiter` only** — `apps/web`/`apps/services`/`apps/api`/`packages/*` **byte-untouched**; **no schema/migration/new flag/shared-surface lock**). Replaces the empty-state stub. **Verification first** (owner requirement): three axes off existing columns — work email (`Recruiter.workEmailVerified`, the only axis that gates posting), company profile (the 8 columns `/profile` edits), company KYC (the **exact 5 submit predicates the API enforces**, mirrored word-for-word so the card can't say "ready to submit" when `submitKyc` would reject). PENDING counts as done from the recruiter's side but never claims "verified"; REJECTED shows the reviewer's reason; killswitch ON drops the step entirely (no dead `/kyc` link, and the all-clear summary is assembled from the steps that actually ran so it can't claim an approval that never happened). **GST/PAN/legal name collapse to booleans in the query layer** — no PII enters the render tree. **KPIs**: jobs posted / open / draft · applications received · needs review · **candidates hired**, plus candidate pipeline, jobs-by-status, needs-attention and top jobs. Company-scoped (`companyId` — `postedById` is nullable and would drop departed posters' jobs); only own rows link to `/jobs/[id]` (404s for others). **Omitted rather than faked**: views/impressions (no such table), time-to-hire (`statusHistory` unpopulated), saved-job interest (0 rows), hire-rate %. **Perf** (the owner's ask — this is the post-sign-in landing page): verification needs 1 query and paints with the shell; the 6 aggregates stream behind `<Suspense>` + a skeleton + a new `loading.tsx` (**first in the repo**); `react cache()` de-dupes the recruiter lookup the layout and page both paid; 2 `groupBy`s collapse 13 counts into 2 round-trips; no per-job fan-out. **Measured: shell 167ms → verification 261ms → KPIs 321ms**, vs 322ms fully blocking. Also **stood up the first vitest harness in `apps/recruiter`** (32 tests; `pnpm test` had silently skipped the workspace) and fixed two pre-existing shell issues: the top-bar `KycStatusBadge` rendered even when its killswitch 404'd `/kyc`, and the shell ran 4 independent awaits in series. **Adversarial 4-lens review (correctness/a11y/isolation/copy, each refuted): 33 raised → 17 confirmed, all fixed** — headline: a filled-but-unsubmitted *and* a REJECTED KYC both hit `done === total`, driving a **100% bar (and "Verification 100% complete" to screen readers) beside a "2 of 3 complete" headline on a just-rejected account**; fixed with one central crediting rule + an invariant test (`percent===100 ⇔ complete`) that then caught the second instance itself. Also: raw GSTIN left the accessor under a comment claiming it didn't; copy promised a verified badge "on your job postings" (none exists), called applications "applicants", and asserted an unrecorded read-state; two comments claimed nothing flips jobs to EXPIRED (**`apps/api/src/job-lifecycle` does**); `first:pt-0 last:pb-0` on a sole-child link zeroed every attention-row's padding (the seeker-rail bug again); `KpiTile`'s aria-label replaced the visible label (WCAG 2.5.3) and dropped every hint; `TopJobs` dropped status from its aria-label; a bar `<div>` sat between `<dt>` and `<dd>`; `role="status"` had only aria-hidden descendants. Gate green (typecheck 11/11, tests 6/6 — API 629 / web 188 / **recruiter 32 (new)** / auth 17 / feature-flags 37 / observability 19, build 4/4). **Browser-verified** (Priya @ Nimbus, real data): counts cross-checked vs Postgres (7 jobs, 105 applications, 1 hired); all four KYC states + killswitch; 0 contrast failures across every text node; no overflow at 1280/768/375; 0 console errors. **DB fully restored** (temp KYC row + 2 documents deleted, killswitch back OFF, 7 ACTIVE Priya jobs, 371 applications). |
| 2026-07-22 | `feature/career-advice-editorial-v3` | **Career Advice hub → all-light editorial redesign v3** (`/career-advice`, SRS §4.8; **apps/web only** — `theme.css`/`packages`/other apps byte-untouched; **no schema/migration/flag/lock**; career-advice barrel + `globals.css` edits append-only). **Replaces the rejected v2** (owner rejected both v1 and v2 as "too simple / poor design"). Owner-approved via a full-fidelity **Artifact** pitch with two hard rules: **(a) purely light** — no dark section/card backgrounds in the light theme (navy/cyan only as ink, marks, one CTA, and the active topic-pill fill); **(b) cover images on every article**. Keeps the real `SiteShell` navbar/footer. `CareerMasthead` (kicker + oversized **serif** "Get hired, on purpose." with "purpose" in `accent-700` italic + `ArticleSearch` + topic pills) → featured `CoverTile size="cover" headingLevel={2}` (horizontal cover image + serif copy) → 2-up `CoverTile size="tile"` grid → **light cyan-tint** `CareerColophon` CTA card. New **`ArticleCover`** renders a real `coverImageUrl` via `next/image` when present, else a **designed on-brand SVG cover** keyed to `tags[0]` (salary/resume/portfolio/applying/early-career + fallback; flat navy/cyan shapes, token-var fills, `aria-hidden` decorative). New scoped **`.font-editorial`** serif rule in `apps/web/app/globals.css` kept **outside `@layer utilities`** (Tailwind 4 tree-shakes custom classes declared inside that layer; also needed a clean dev restart to apply). Retired the v2 **`ContentsRail`** + **`article-visuals.ts`** navy-plate system; shared **`ArticleCard` untouched** (homepage safety). `?q=` search + `?tag=` filter + featured/empty-state (`gridArticles.length===0 && !featured`) + archive logic preserved. Data reality unchanged (3 published articles, **all `coverImageUrl=NULL`** → designed SVG covers render; 1 author; ~5 tags; no fabricated photos). **Adversarial 4-lens review: 0 confirmed findings.** No test files changed (`params.ts` untouched → web **188**). Gate green (typecheck 11/11, tests 5/5 — API 629 / web 188 / auth 17 / feature-flags 37 / observability 19, build 4/4). **Browser-verified** (real data): **0 dark section backgrounds** in light theme; the `.font-editorial` serif applies; the designed SVG covers render for all 3 tag-keyed articles; all text AA (kicker/tags `accent-700` 5.58–5.84:1, titles/CTA/active-pill 15–18:1, colophon on the cyan tint 7.29–16.75:1); no horizontal overflow at 1360/768/375; the topic pills filter correctly; the homepage `ArticleCard` unaffected. |
| 2026-07-20 | `feature/career-advice-editorial` | **Career Advice hub → editorial-magazine redesign v2** (`/career-advice`, SRS §4.8; **apps/web only** — `theme.css`/`globals.css`/other apps/packages untouched; no schema/flag/lock). Owner found the v1 index "too simple" + wanted visuals despite the 3 articles having **no cover images**. Judge-panel (4 concepts → 3 judges) winner **"The Editorial"** + **"Index Plate"** grafts: **masthead** (issue-line + oversized wordmark + search) → desktop **contents rail** (mobile TagFilter fallback) → asymmetric featured **cover plate** → 2-up **CoverTile** grid → **navy colophon** CTA. The image-less fix = new **`CoverTile`**: a designed cover from real metadata — flat navy/tint FIELD (3 deterministic variants ink/paper/rule + adjacent-collision tie-break), a large **per-topic glyph** (salary→₹, resume→file, portfolio→briefcase…), a decorative bled **folio numeral** (aria-hidden, never a step), a flat **SVG dot `<pattern>`** texture (a vector shape, NOT a gradient), a cyan editorial rule, title set big as artwork; renders a real `coverImageUrl` when present. New `CareerMasthead`/`ContentsRail`/`CareerColophon`/`article-visuals`; retired `FeaturedArticle`/`CareerHero`/`CareerSidebar` (subsumed); **shared `ArticleCard` UNTOUCHED** (homepage safety). 100% real data, no fabricated imagery/gradients/glassmorphism. **Adversarial 4-lens review: 2 issues → both fixed** — (MED, 4 lenses) featured cover emitted `h3` under the masthead `h1` (heading skip) → `CoverTile` gains a `headingLevel` prop, lead story is now `h2` (grid cards stay `h3`); (LOW) default 0-articles view showed the *filtered* "Nothing matches" copy → branched on `q||tag`. Also fixed a `fg-subtle`→`fg-muted` contents-rail count in self-audit. Gate green (typecheck 11/11, tests 5/5 — web 188, build 4/4). **Browser-verified** (real data): featured ink plate renders dots+glyph+"01"; two grid tiles show **different variants** (paper + rule-with-cyan-spine); heading order `[1,2,2,3,3,2]` **no skip**; **no overflow at 1360/768/375** (featured title clamps 56→30px mobile); all text AA (ink kicker/CTA 8.7:1, ink title 19.5:1, ink excerpt 12.7:1, paper kicker 5.35:1, rail active 15.4:1, masthead 5.84:1, colophon 8.7–16:1); search+tag filter work (no featured when filtered, rail active state); default empty-state verified (unpublished all 3 → "No articles yet", then **restored**); 0 console errors; homepage `ArticleCard` unaffected. |
| 2026-07-20 | `bugfix/company-logo-image-host` | **Fix: web `CompanyLogo` crashed `next/image` on API-served logos** (`apps/web` only; 1 prop + comment; no schema/flag/lock). A recruiter-uploaded logo is served from the API `/media` passthrough (`http://localhost:4000/media/company-logos/…` in dev), whose host is **not** in `next.config` `images.remotePatterns` → the web `CompanyLogo`'s optimized `next/image` threw *"Invalid src prop … hostname 'localhost' is not configured"* and crashed the **seeker homepage** (`FeaturedJobs → FeaturedJobCard → CompanyLogo`) + every page rendering that company's logo (SRP, `/companies`, company detail). Added **`unoptimized`** — small already-sized logos served as-is, bypassing the optimizer + host allowlist; **mirrors `apps/recruiter/components/CompanyLogo.tsx`, which already does exactly this**. Fixes all web `CompanyLogo` usages at once. Gate green (typecheck 11/11, tests 5/5, build 4/4 on the integrated state incl. the teammate's plans-always-visible merge). **Browser-verified** by temporarily giving company 7 (Rasta Logistics) the API-media logoUrl from the error report: company detail + homepage FeaturedJobs render the logo as a raw unoptimized `<img>` (`isNextOptimizerProxy: false`), **no "hostname not configured" error**, 0 console errors; DB restored (logoUrl → NULL). |
| 2026-07-20 | `feature/recruiter-plans-always-visible` | **Recruiter Plans & Billing visible to every recruiter, with Free as the default plan** (SRS §4.11/§7; recruiter + API + packages; **`apps/web`+`apps/services` byte-untouched**; **no schema/migration**). Splits **VISIBILITY from PURCHASABILITY**. New flag **`recruiter.plans_visible`** (**keys.ts lock taken+released**), seeded **ON ⇒ 41 flags**, now gates L1 (`middleware.ts`), the sidebar Billing group (`layout.tsx`→`SidebarNav showBilling`) and L2 (`plans`/`billing` pages) — all three previously read `subscription.system.enabled`. **`subscription.system.enabled` is deliberately NOT flipped**: `apps/api/applications/quota.service.ts` feeds it into `apps/web`'s `ApplyButton` as `upgradeAvailable`, which would show rate-limited **job seekers** an "Upgrade → See plans" CTA linking to **`/pricing`, a route that does not exist in `apps/web`** — the decoupling is what keeps the seeker portal clean. It now means *purchasable* only, still enforced at L3 (`assertBillingEnabled`, untouched). The per-tier `subscription.plans.<tier>.enabled` keys **no longer filter cards out**; they compute a per-plan `purchasable` that renders a **"Coming soon"** status line instead of a CTA. New synthetic **`FreePlanCard`** (no DB row — a company with no `Subscription` already resolves to FREE) badged "Current plan"; `/plans` opts into `data-wide` so all four cards fit one row. RECRUITER plans now ship **listed** (`isActive`/`isPublic`); **CANDIDATE plans stay dark**. **Adversarial 4-lens review (security / react-a11y / isolation / copy-truthfulness, each refuted): 13 raised → 8 confirmed, all fixed** — HIGH the purchase-closed banner ignored `currentPlanId` so a paying company was told it was "on the Free plan" while its paid card showed "Current plan"; HIGH plan copy advertised **ResDex / bulk messaging / priority support (all unbuilt)** and "unlimited postings" (contradicting the deliberately careful quota label) — corrected, and RECRUITER `name`/`description` now re-sync on reseed so stale copy can't survive; MED `/billing` exposed a billing-details form the API 403s on Day 0 (now hidden while purchasing is closed); MED the seed's blanket `updateMany` overrode admin intent every reseed (narrowed to the unlisted default, so a deliberate delist survives); MED a Member saw a footer on unbuyable cards but none on buyable ones; plus present-tense free-promise copy, a `cache-purge` entry so the flag can't purge the **seeker homepage** (+1 test), and stale-comment fixes. Disabled-button contrast (3.39:1) avoided by rendering status text rather than a dead control → **7.79:1**. Gate green (typecheck 11/11, tests 5/5 — **API 629** (+1) / web 188 / auth 17 / feature-flags 37 / observability 19, build 4/4). **Browser-verified** (Priya @ Nimbus, true Day-0 flag state): Billing group appears with billing OFF; Free badged "Current plan"; 3 paid cards priced with "Coming soon"; **MEMBER sees the identical page** (role temporarily flipped, restored); both banner branches; OWNER+flags-ON still yields real "Choose plan" buttons (purchase path intact); **killswitch OFF → `/plans`+`/billing` 404 and the nav group vanishes** while `/dashboard`+`/jobs` stay 200; near-expiry job's Upgrade CTA now reaches a live `/plans`; 4-up/2x2/1-up at 1280/768/375 with no overflow; dark mode AA (10.2–17.2:1); 0 console errors. **DB fully restored** (temp subscription deleted, Priya OWNER + 7 ACTIVE jobs, `subscription.system.enabled` back **OFF** — note it had been left ON locally by an earlier demo). **Follow-ups:** Enterprise still lists ResDex/bulk-messaging *bullets* via `featureKeys` (product call — left for the owner); the new flag is intentionally **not** critical because syncing `CRITICAL_FLAG_KEYS` would require editing `apps/web/lib/admin/types.ts`. |
| 2026-07-19 | `feature/career-advice-redesign` | **Career Advice hub redesign** (`/career-advice`, SRS §4.8; **apps/web only** — `theme.css`/`globals.css`/other apps/packages byte-untouched; no schema/migration/flag/lock). Focused premium editorial index (owner scope: omit no-data sections, focused index, typographic image-ready). Wrapped in **`SiteShell`** (global nav+footer — fixes the bare-`<main>` chrome gap; route now dynamic, `revalidate` dropped). **Editorial hero** + **real `?q=` article search** (title/excerpt contains, prefilled, clear button) → **typographic image-ready featured lead story** (newest; auto-shows a cover if one's ever added) → **2-col**: "Latest articles" grid + **sticky sidebar** (Browse-by-topic real tags+counts / Recently published / real Browse-jobs+Job-alert CTA). Topic nav swaps to a horizontal chip row on tablet/mobile (sidebar hidden). New `CareerHero`/`ArticleSearch`(client)/`FeaturedArticle`/`CareerSidebar`/`article-format` in `components/career-advice`; enhanced the **shared `ArticleCard`** (whole-card stretched link, category badge, author monogram, fg-muted meta — also used by the homepage articles section, verified no regression); `params.ts` +`?q=` parsing (+2 tests → web 188). **100% real data** — omitted newsletter/popular-authors/roadmaps/learning-collections/career-guides/free-resources/related-services/most-read/trending/views/bookmark (no backing). **Adversarial 4-lens review** (correctness/a11y/brand-contrast/isolation, each refuted): **1 confirmed → fixed** (empty-state fired below the featured card when exactly 1 article is published → gated on `&& !featured`; browser-verified by temporarily unpublishing 2 articles, then restored). Also fixed a `fg-subtle`→`fg-muted` topic-count in self-audit. Gate green (typecheck 11/11, tests 5/5 — web **188**, build 4/4). **Browser-verified** (real data): search (`?q=salary`→1) + tag (`?tag=applying`→2) both work + compose + drive sidebar active state; featured hidden when filtered; layout `824/280` desktop → sidebar folds → mobile chips; **no horizontal overflow at 1360/768/375**; all text AA (eyebrow 5.58 / featured-label 5.84 / badges 15.2 / meta 7.79 / CTA 15.4); 0 console errors; homepage articles section unaffected. |
| 2026-07-19 | `feature/navbar-hire-cta-restyle` | **Navbar employer CTA restyle + rename** (shared `SiteHeader`/`HeaderAuthActions` + `MobileMenu`; all public pages). The "Hire on Career Queue" button was too wide → rephrased to **"Hire talent"** (same meaning, ~250px→123px) and restyled from a solid cyan fill to a **cyan outline that fills on hover** (rest: white bg + cyan border + cyan label; hover: solid cyan fill + white text; 150ms ease-out). **Owner-directed colour: the EXACT brand cyan `--color-accent-500` (#22A0DA, the logo-arrow cyan)** on all three cyan surfaces — a deliberate brand-fidelity choice over the AA-safe deeper stops (contrast lands ~2.95:1, below AA 4.5; navy-on-cyan reference is 7.11:1 if readability ever needs it). Mobile-drawer CTA kept consistent (filled accent-500 + white). `apps/web` only — no schema/flag/lock; theme untouched. Gate: typecheck ✓ + build 4/4 ✓ (className/text-only → unit tests unaffected). Browser-verified (exact `#23a0da` computed, 123px width, 150ms transition, 0 console errors). |
| 2026-07-19 | `feature/home-redesign` | **Search-first homepage redesign + flat brand + Latest Jobs** (`apps/web/app/page.tsx`, SRS §4.1; **apps/web only** — navbar + footer + `theme.css`/`globals.css` + packages/other apps byte-untouched; no schema/migration/flag/API/lock). Design via a **judge-panel Workflow** (3 concepts → 3 judges → unanimous winner "The Clean Index"). **Flattened every content section** off the banned gradients + glassmorphism onto the flat navy/cyan borders-over-shadows brand (`.glass`/`.gradient-brand(-soft)`/`gradient-border`/`card-lift` usage → flat bordered surfaces + `primary-100` icon chips + flat accent ticks; removed radial cyan washes + blur blobs; `RecruiterCta` → flat deep-navy #192249 finale). Tokens/utilities LEFT DEFINED (still consumed by SrpSearchExpand/AuthModal/OnboardingWizard/CitySelect/footer + theme.css is §15-locked). **New search-first IA:** Hero (search + live count ribbon) → **Latest jobs (8 real cards)** → Companies → Roles → Cities → Industries → Skills → Why(BentoValue) → Articles → FAQ → Recruiter. **Added the missing Featured/Latest Jobs section** (the real IA gap): repurposed the formerly-dead `heroJobs` query (`take 3→8`, `HeroJob`→`FeaturedJob`, `heroJobs`→`latestJobs`), new flat **`FeaturedJobCard`** reusing the shared paise `formatSalaryLpa`/`postedAgo`/`WORK_MODE_LABELS`, **deleted** the dead glass `HeroJobCluster`. **Refreshed the hero** ("Find your next job in India.", flat search bar, flattened chips) and **folded the standalone `TrustStrip`** stats into a slim live count ribbon under the search (freemium message preserved; TrustStrip deleted). Bumped several `fg-subtle` labels → `fg-muted` for AA. **Omitted** testimonials/newsletter/app (no backing data — owner decision). **Adversarial 4-lens review** (correctness/a11y/brand-contrast/isolation, each refuted): **1 confirmed → fixed** (a stale "glass chips" comment); all substantive lenses clean. Gate green (typecheck 11/11, tests 5/5 — web 186, build 4/4). **Browser-verified** (real data, anon): search-first order exact, 8 jobs at 4/2/1 cols, count ribbon, **0 `backdrop-filter`** (glass fully gone, only the neutral dot texture remains), **no horizontal overflow at 1360/768/375**, all text AA (headline 3.82:1 large / ribbon 5.58:1 / salary 17.9:1 / skill-count 7.14:1 / recruiter eyebrow 6.89:1 / New badge 5.46:1), 0 console errors, and the **shared `HeroSearchBar` still flat + correct in the SRP expanding-search** (no cross-surface regression). |
| 2026-07-19 | `feature/company-detail-redesign` | **Company Detail page redesign** (`/company/[handle]`, SRS §4.7; **apps/web only** — `apps/services`+recruiter+api+packages byte-untouched; no schema/migration/flag/lock). Reworked the seeker company profile from a single-column bare-`<main>` stub (no site chrome) into a premium, `SiteShell`-wrapped enterprise layout: a content-first **hero** (logo · name · dormant-until-KYC **Verified badge** · industry + company type · rating · fact row · **Share** + **View jobs**) + a real-metrics **stat strip**, then a responsive **3-column grid** — sticky scroll-spy **nav** + **Quick Facts** (left) · About / Highlights / Open positions / Reviews (main) · sticky **Hiring** snapshot + **Related Companies** (right). Sidebar content folds inline below its breakpoint (pure components; the related-companies query resolves once and is shared). New `CompanyProfileHero`/`CompanyStatStrip`/`CompanyShareButton` (client, Web Share + copy fallback)/`CompanyProfileNav` (client scroll-spy)/`CompanyQuickFacts`/`CompanyHighlights` (renders `workingAtSections` when present)/`CompanyHiringRail`/`RelatedCompanies` (same-industry peers + live-role counts)/`VerifiedBadge`/`company-format.ts`. `CompanyAbout`/`CompanyOpenings`/`CompanyReviews` gained card wrappers + section anchors (logic unchanged). Wrapping in `SiteShell` makes the route **dynamic** (revalidate dropped — same trade-off `/companies` + `/job` took); JSON-LD (org + breadcrumb) + self-canonical + slug-drift 308 preserved. **No fabricated data** — follow/followers/gallery/timeline omitted (no backing model); verification/company-type/highlights built but dormant until a recruiter fills them. **Adversarial 4-lens review (correctness/a11y/brand-contrast/isolation, each finding refuted): 1 confirmed → fixed** (invalid `<dl>`: `<p>` sub-text sibling moved inside `<dd>`). Also fixed 2 contrast issues found during self-audit (green "Active" stat 2.77:1 → fg text + colored dot; `fg-subtle` misused for real text → `fg-muted`). Gate green (typecheck 11/11, tests 5/5 — API 628 / web 186, build 4/4; `/company/[handle]` now `ƒ` dynamic). **Browser-verified** (Nimbus + Sahaj Pay): layout `220/1fr/300` desktop, exactly one instance of each folded section, **no horizontal overflow at 1360/768/375**, dormant paths light up when data present (temp KYC+type → badge+type rendered → **DB restored**), all text AA (Active 16.4:1, labels/meta 7.79:1, website link 5.84:1, badge 15.2:1), 0 console errors, `working-at` (shares old `CompanyHero`) still 200. Scroll-spy active-highlight not frame-verifiable in the hidden preview tab (IO paused) — standard IO, works in a real tab. |
| 2026-07-18 | `feature/recruiter-job-collaborate` | **Recruiter Job Detail — Collaborate (per-job collaborators)** (SRS §4.9; recruiter + API + packages; **`apps/web`+`apps/services` byte-untouched**). PR B of the job-detail enhancement — lets a job **owner** add company teammates as **collaborators** who can then **open, manage (edit/close/reopen) and respond to (act on applicants)** the job. **Schema** (migration `add_job_collaborators`, **DB lock taken+released**): new **`JobCollaborator`** model (`jobId`→Job, `userId`→User, `addedById`, `@@unique([jobId,userId])`; keyed by User id to match `postedById`/JWT `sub`) + **`NotificationType.JOB_COLLABORATION`**. **Flag** (**keys.ts lock taken+released**): **`killswitch.recruiter_job_collaborate`** seeded OFF ⇒ live ⇒ **40 flags** — L3 503 on add/remove, L2 hides the Collaborate control. **API**: new `recruiter-job-collaborators` module — **GET/POST/DELETE `/recruiter/jobs/:id/collaborators`** (owner-only; POST validates same-company + active recruiter, idempotent, notifies the teammate via `notifyJobCollaboration`). A shared **`jobManageableWhere()`** broadens the owner-only guards to **owner-OR-collaborator** across `recruiter-jobs` (getOne/update/close/reopen — **delete/publish stay owner-strict** via a new `getOwnedJob`) and `recruiter-applicants` (list/transition/notes/resume); the **detail/applicants/edit RSC pages** broaden their guards too. **UI**: `PostedByCard` gains collaborator avatars + an owner-only `CollaborateDialog` (add/remove from company teammates); new shared `PersonAvatar`. **+13 API tests → 628**. Gate green (typecheck 11/11, tests 5/5, build 4/4). **Browser-verified** (Priya @ Nimbus, with Rohan Mehta temporarily moved into Nimbus): assignable list → **Add** (creates row + JOB_COLLABORATION notification linking `/jobs/100001`) → Rohan (collaborator, non-owner) **can now open** `/jobs/100001` **and** `/jobs/100001/applicants` (both previously 404), sees **no** owner-only Collaborate button, and **404s on a non-collaborated job (100002)**; **Remove** deletes the row; **L2 killswitch** hides the control when ON. **DB fully restored** (Rohan → Veridian, 0 collaborator rows, 0 test notifications, flag OFF, Priya's 7 jobs ACTIVE). |
| 2026-07-18 | `feature/recruiter-job-detail-actions` | **Recruiter Job Detail — Quick Actions + Email share + Posted-By card** (SRS §4.9; recruiter-only; **no schema/migration/flag/shared-surface-lock**; `apps/web`+`apps/services` byte-untouched). PR A of the job-detail enhancement (the Collaborate RBAC half is PR B, planned above). **①** New `JobQuickActions` client bar in `JobDetailHeader` mirroring the Jobs-list ⋮ action set — **View applicants · Edit · Share · Duplicate · Close/Reopen · View public page** — reusing `JobStatusDialog` + `ShareJobDialog` + the `/post-job?duplicate=<id>` deep-link; status-gated exactly like `JobRowMenu` (Share for ACTIVE; Close for ACTIVE/EXPIRED; Reopen for CLOSED/EXPIRED). Delete/Publish deliberately stay in the list menu, not this prominent bar. **②** `ShareJobDialog` gains an **Email** (`mailto:` subject+body) channel → LinkedIn/X/WhatsApp/**Email**/Copy (benefits the Jobs-list menu too). **③** New `PostedByCard` sidebar card — poster **name** (`User.name`), **photo** (`User.image` → initials fallback) and **designation** (`Recruiter.designation`); detail RSC `select` widened with `postedBy` (handles null poster). Gate green (typecheck 11/11, tests 5/5 — API 615, build 4/4). Browser-verified as Priya @ Nimbus (job 100001): all 6 action buttons + correct hrefs, Share dialog's 5 channels incl. Email `mailto:`, Close confirm opens+cancels (demo job untouched), Posted-By PS/Priya Sharma/designation, 0 console errors, no horizontal overflow at 1280/375. |
| 2026-07-18 | `feature/recruiter-job-detail` | **Recruiter Job Detail page** (recruiter-only; **no schema/migration/flag/API/shared-surface-lock**; `apps/web`+`apps/services` byte-untouched). New owner-scoped RSC at **`/jobs/[id]`** — opened when a recruiter clicks a published job's title on the Jobs list (`JobsTable` own-row title link repointed `/jobs/[id]/applicants` → `/jobs/[id]`; teammate rows stay unlinked). Six sections (SRS §4.9): **Overview header** (title/company/location/job type/openings/posted/status badge + View-applicants/Edit/View-public actions), **Job description** (plain-text render of the `description` column — the rich `descriptionMarkdown` pipeline is apps/web-only; shared-package promotion is a follow-up), **Candidate requirements** (experience/skills/education/department/type), **Salary** (₹min–max LPA, or **"As per industry standards"** when no range is set — the Job model has no confidential flag), **Application statistics** (Responses/New/Shortlisted/Rejected/Matches via one `application.groupBy` + one skill-overlap `count`, each deep-linking into the filtered applicants list), and **Posting validity/expiry** with days-remaining + status-aware Extend (→ edit) / Upgrade (→ /plans, gated on `subscription.system.enabled`) CTAs when nearing expiry. Also adds a **`rejected`** applicant filter (`applicant-filter.ts` + applicants page + `ApplicantsFilterTabs`) so the Rejected stat is clickable; appends salary/experience/employment formatters + `daysUntilExpiry` to `job-list-format.ts` (mirrored from apps/web — can't import across apps). **Adversarial review (3 lenses → per-finding refuter): 9 raised → 8 confirmed, all fixed** (MED status-aware validity copy/CTA for CLOSED/DRAFT; MED `daysUntilExpiry` ceil→floor; MED expired-danger WCAG-AA contrast via theme-aware `color-mix`; LOW sticky-sidebar offset; NIT external-link new-tab AT hint; NIT applicants id-guard hardening). Gate green (typecheck 11/11, tests 5/5 — API 615, build 4/4). Browser-verified as demo recruiter Priya @ Nimbus: all 6 sections, stats cross-checked vs Postgres (16/9/0/0/5 exact), confidential/openings/near-expiry/expired/CLOSED/DRAFT/billing-gate/rejected-filter, danger contrast 7.0/6.7:1, no console errors, no horizontal overflow at 1280/375, sticky 2:1 grid → single column mobile, dark mode; **DB restored to exactly 7 ACTIVE Priya jobs**. |
| 2026-07-17 | `feature/recruiter-create-publish-fields` | **API: create(PUBLISH) now requires city/department/openings** (API-only; no schema/migration/flag/lock; `apps/web`+`apps/services` untouched). Closes the trust-boundary gap the publish-draft review surfaced: a direct `POST /recruiter/jobs` with `{ publishMode:'PUBLISH', title, description }` could create a live ACTIVE job with no city/department/openings (the wizard gated these only client-side; CLAUDE.md §4 — the API is the only trusted enforcement point). `CreateRecruiterJobDto` gains a `publishMode==='PUBLISH'` **refine** (Zod 4 dynamic `error` callback naming only the actual gaps); **DRAFT stays lenient** (title + description only). Extracted a shared **`missingPublishFields()`** helper (dto.ts) used by BOTH the DTO refine and `publish()` so the two make-live paths can't drift (publish()'s message is byte-identical). +9 dto tests (16 total). Adversarial review (1 focused agent, Zod-4-empirical): **no defects** — no bypass/false-positive, DRAFT lenient, composes with the salary/experience refines + `.strict()`, callback never throws on partial/non-object input. Gate green (typecheck 11/11, tests 5/5 — API **615** (+9), build 4/4). Browser-verified the wizard's normal Publish still works (job→ACTIVE, no error) + DB restored to 7 jobs. Follow-up from `feature/recruiter-publish-draft` — **now closed.** |
| 2026-07-17 | `feature/recruiter-publish-draft` | **Recruiter Publish-from-draft (DRAFT→ACTIVE)** (recruiter-only; `apps/web`+`apps/services` byte-untouched; **no schema/migration/new flag/lock**). Closes the gap the Draft Jobs list exposed: a saved DRAFT could be edited/duplicated/deleted but never published. New **`POST /recruiter/jobs/:id/publish`** (bodyless): reuses the existing **`killswitch.recruiter_post_job`** (L2 hides the menu item, L3 503) + create()'s verified-work-email gate, 404s on non-ownership, rejects non-DRAFT, **validates the stored draft's mandatory fields (title/description/department/openings/city) and 400s with the gaps BEFORE consuming quota**, then consumes the post quota, honours the moderation flag (ACTIVE vs PENDING_MODERATION), refreshes `postedAt` to go-live, clears a stale past `expiresAt`, and fires the publish side-effects (ES index + alerts + cache purge) + job-posted email. The DRAFT→final flip is an **atomic `updateMany` guarded on `status:'DRAFT'`** (the delete() pattern) so a double-click/retry race can't double-consume or double-email — the loser refunds its slot; the failure path refunds too. Recruiter ⋮ menu gains a **Publish** item (own DRAFT rows only, first action) via a new `JobStatusDialog` `publish` action; `packages/ui/icons.ts` +`Send` (append-only). **Adversarial review (3 lenses → per-finding verify): 11 raised → 8 confirmed, all fixed** (atomic flip + refunds, verified gate, stale-expiry clear, moderation-accurate dialog copy, comment fix, +test coverage). Gate green (typecheck 11/11, tests 5/5 — API **606** (+10), build 4/4). Browser-verified end-to-end as Priya @ Nimbus (publish item DRAFT-only; complete draft→ACTIVE + quota 0→1; incomplete→400 "missing: department, city" + quota NOT consumed; atomic path re-verified post-fix; **DB restored to 7 jobs**; 0 console errors). **Follow-up:** create(PUBLISH) DTO leaves city/department/openings optional (wizard gates them client-side) — publish() is intentionally stricter; tightening create() is a separate task. |
| 2026-07-17 | `feature/recruiter-draft-jobs` | **Recruiter "Draft Jobs" nav sub-item + Save-as-Draft fix** (recruiter-only; no schema/migration/flags/shared-surface-locks; `apps/web`+`apps/services` byte-untouched). **Nav**: the sidebar **"Jobs"** item became a **collapsible disclosure group** (same pattern as Settings/Billing/Help) with children **"All jobs"** → `/jobs` and **"Draft Jobs"** → `/jobs?status=DRAFT` — Draft Jobs **reuses the existing Jobs list** (filter bar, sort, pagination, per-row ⋮ actions) pre-filtered to `status=DRAFT` rather than duplicating a page. `SidebarNav` gains `useSearchParams` for query-aware active state (only one child is `aria-current` at a time; parent highlights on any `/jobs`/`/jobs/*` route). **Save-as-Draft fix**: the wizard enabled "Save as draft" on **title only** and claimed "a draft only needs a title", but the create API requires `description` (min 10) for **every** publishMode and `Job.description` is non-nullable, so a title-only draft **400'd**. The draft button now also requires a description (`disabled={!titleOk \|\| !descriptionOk}`), with corrected helper copy + comment. Browser-verified end-to-end (demo recruiter Priya @ Nimbus): Jobs group expands to All jobs/Draft Jobs; Draft Jobs → `/jobs?status=DRAFT` with Status=Draft preselected + correct active state; draft button disabled title-only → enabled with description; created a draft → appeared under Draft Jobs (1 job) → **deleted via the ⋮ menu, DB restored to exactly 7 jobs**; 0 console errors. Gate green (typecheck 11/11, tests 5/5, build 4/4). **Follow-up noted**: a saved DRAFT can't currently be published via any recruiter action (edit=PATCH never changes status; close/reopen reject DRAFT). |
| 2026-07-17 | `feature/recruiter-jobs-action-menu` | **Recruiter `/jobs` → per-row 3-dot action menu (⋮)** (recruiter-only; no schema/migration; `apps/web`+`apps/services` byte-untouched; packages additive-only). Items by status/ownership: **Preview / View public job page** (`{WEB_URL}/job/<canonicalSlug>`, new tab), **Share on social media** (LinkedIn/X/WhatsApp intents + copy-link dialog, ACTIVE only), **Edit** (new `/jobs/[id]/edit` reusing `PostJobWizard` in edit mode → the previously UI-orphaned `PATCH /recruiter/jobs/:id`; blank clearable fields PATCH `null` = clear; min≤max refines added), **Duplicate** (`/post-job?duplicate=<id>` auto-runs the template deep-copy), **Close/Reopen** (canonical confirm Dialogs — `window.confirm` + `JobActions.tsx` retired; EXPIRED-reopen warns about the stale expiry date), **Delete** (new `DELETE /recruiter/jobs/:id` — own jobs with **0 applications** only via an atomic `deleteMany` guard, 409 otherwise; new `killswitch.recruiter_job_delete` seeded OFF ⇒ live, L3 503 + L2 hide). Menu = hand-rolled `role=menu` on the portalled Radix Popover (no new dep): roving arrow/Home/End/Space keys, focus-visible ring, Escape/Tab close. Teammate rows get a reduced read-only menu. `api()` now maps rejected fetches to `{ok:false,status:0}` (spinner can't hang — also fixes pre-existing RemoveUserDialog). Adversarial review: **29 raised → 26 confirmed (15 unique), all fixed & re-verified** (2 HIGH a11y: invisible keyboard focus, sub-AA danger text). Browser-verified end-to-end incl. edit/duplicate/delete/close/reopen cross-checked vs Postgres, killswitch L2+L3, teammate reassignment, mobile 375px, 0 console errors; **DB restored to exactly 7 jobs / flag OFF**. Gate green (typecheck 11/11, tests 5/5 — API 596 (+12), build 4/4). +6 icons appended to `packages/ui/icons.ts`; `NEXT_PUBLIC_WEB_URL` documented in `.env.example`. |
| 2026-07-16 | `feature/recruiter-jobs-sort-pagination` | **Recruiter `/jobs` → sorting + full pagination controls** (recruiter-only; no schema/migration/flags/locks; `apps/web`+`apps/services`+`packages` byte-untouched). **Sort by Posted Date / Title / Status** via **clickable column headers** (direction toggle + arrow + `aria-sort`; default `posted_desc` dropped from URL; deterministic `id` tiebreakers; status sorts by enum lifecycle order Draft→Pending→Open→Expired→Closed) + a **mobile-only Sort-by select** (cards have no headers). **Pagination**: numbered page links with ellipsis windowing + Prev/Next + `aria-current="page"`, plus a **Results-per-page select (10/20/50, allow-listed server-side, default 20 dropped from URL)**; over-range `?page` → redirect to the real last page; `?page` clamped to 1e6 (Prisma i64 `skip` guard); filter-bar `clearAll` now preserves sort/perPage (display prefs, not filters). New `jobs-list-params.ts`/`JobsSortHeader`/`JobsSortSelect`/`JobsPagination`; old inline Newer/Older `PageLink` retired. Adversarial review: **6 raised (1 dup), 6 confirmed, 4 fixed & re-verified** (MED 320px WCAG-1.4.10 wrap; LOW i64-overflow 500; LOW ARIA-prohibited label on disabled span; LOW mobile replace-vs-push); 1 pre-existing clearAll/debounce race deferred as a follow-up task. Sorts cross-checked vs Postgres (exact, incl. page-seam integrity, via 75 temp fixture jobs — created + deleted, DB restored); gate green (typecheck 11/11, tests 5/5, build 4/4); 0 console errors; no horizontal pane scroll at 320/375/520/780/1280. |
| 2026-07-12 | `feature/recruiter-jobs-candidate-counts` | **Recruiter `/jobs` → candidate-count columns** (recruiter-only; no schema/migration/flags/locks). Each job row now shows four clickable metrics — **Responses** (all applications) · **New** (`APPLIED`) · **Shortlisted** (`SHORTLISTED`) · **Matches** (applicants whose candidate skills overlap the job's `skillIds`). Counts computed server-side in the RSC (one `application.groupBy(['jobId','status'])` + bounded per-job matched `count`s) — no API change (reads/writes split). Each number links into the applicants list, which gains an additive `?filter=new\|shortlisted\|matched` param + an `ApplicantsFilterTabs` (All/New/Shortlisted/Matches) row + filter-aware header/empty copy + filter-preserving pagination. Own jobs link; teammate/zero counts render as plain text (applicants page is owner-private). The 9-column table would have overflowed the shell's `max-w-3xl` cap → the Jobs page opts into the **wider content column** (`data-wide` → layout `max-w-6xl`), and `body:has([data-wide])` clips document-level horizontal overflow **only for that page** (table still scrolls inside its own card; other recruiter pages unchanged). New `JobsTable` metric columns + `applicant-filter.ts` + `ApplicantsFilterTabs.tsx`. Adversarial review: **3 findings fixed** (zero-count WCAG-AA contrast, filter-tab narrowing, keyboard-scrollable region). Counts cross-checked vs Postgres (all 7 demo jobs exact); browser-verified filters/links/empty-states + no horizontal page scroll at 375/1024/1280/1440; gate green (typecheck 11/11, tests 5/5, build 4/4). `apps/web`+`apps/services` byte-untouched. |
| 2026-07-12 | `feature/srp-expanding-search` | **SRP click-to-expand search** (SRS §4.1). New `SrpSearchExpand` (components/srp): at rest it mirrors the compact `withButton` search bar; clicking it **pops out** (opacity+scale, 200ms ease-out, elevated popover) into the home hero's 3-field `HeroSearchBar` (skills/location/experience), **prefilled** with the current query + auto-focused, popping out **wider** (max-w-4xl) than the 768px resting bar so the fields don't clip. Collapses on outside-click / Escape (focus returns to trigger) / new search (keyed by query). Always-mounted popover toggled with **`inert`** + opacity/scale (smooth both ways; fields kept out of tab order/AT while shut). `HeroSearchBar` gains optional `initial{What,Where,Exp}` props (backward-compatible → home unchanged); `SrpShell` swaps `SearchInput`→`SrpSearchExpand`. Browser-verified rest/open/collapse/prefill/focus + mobile stack + no overflow (1320/375), console clean. `apps/web` only — no schema/theme/flags/locks. |
| 2026-07-12 | `feature/home-hero-copy` | **Home hero rework** (`Hero.tsx` + `HeroSearchBar.tsx`, SRS §4.1). Headline **"Where great careers begin."** (word "careers" in flat brand cyan `accent-600`, 4.03:1 AA), size reduced 72→48px, gradient text removed. Subtext → **"Real jobs, real companies. No ads."** Removed the navy→cyan **gradient mesh + cyan aurora** (brand: flat colours, no gradients); kept only the neutral dot texture. Hero is now **content-driven + top-weighted** (no forced full-viewport height → no empty void when shifting content up). `HeroSearchBar`: **wider bar** (`max-w-3xl`→`4xl`), **16px semibold** input labels, "what" field widened so the placeholder fits. Iterated live with the owner; browser-verified 1320px + 375px (no overflow, console clean). `apps/web` only — no schema/flags/locks. |
| 2026-07-12 | `feature/companies-directory-redesign` | **Full `/companies` redesign** (seeker directory, SRS §4.7). Wrapped in `SiteShell` (shared header+footer) → page is now **dynamic** (server-side auth header), `revalidate` dropped. New **`IndustryShowcase`** (horizontal category-card rail, real per-industry counts, faint navy band + cyan counts, scroll arrows), **`CompanyFilters`** (elevated faceted-filter card: Sort/Availability/Industry radio+checkbox, active-count badge, count pills; URL-driven; renders **bare** in the mobile `MobileFilterSheet`), and a **redesigned `CompanyCard`** — "The Ledger" data-console tile (chosen via a 4-concept design panel): hairline-divided `<dl>` (Open roles/Rating/Location) on a navy tint, bold headline + navy kicker-tick, cyan "Hiring" tag, ghost "View jobs". `params.ts` gains `sort`+`hiring`+`buildDirectoryQuery`. **Adversarial review: 15 findings, 14 fixed** (over-range `?page` redirect, deterministic `id` tiebreaker, aria-current not aria-pressed, fg-subtle→fg-muted AA, showcase preserves filters, sr-only heading, etc.). Browser-verified desktop + 320px (no overflow), console clean. `apps/web` only — no schema/theme/flags/locks. |
| 2026-07-12 | `feature/recruiter-jobs-filters` | **Recruiter `/jobs` → Filters & Search bar** (recruiter-only; no schema/migration/flags/locks). New `JobsFilterBar` client component above the table: debounced **search by job title OR numeric Job ID** + **Status / Category (jobType: Free·Hot Vacancy·SMB·Internship) / Location / Posted-by** dropdowns. URL/searchParams-driven (mirrors the retired `JobsStatusFilter` pill row, now **folded into the bar**); `PageLink` preserves all params across pagination. The Jobs list is **broadened from own-jobs to company-wide** (`companyId`) so Posted-by has teammates to list; Close/Reopen **and** the title/applicant-count links are `isOwn`-gated (the applicants page + close/reopen API are own-jobs-only → teammate rows are view-only plain text, no 404 links). New `JobLink` helper + `JOB_TYPE_LABELS`; `JobsStatusFilter` retired. Adversarial review: **6 findings, all fixed & re-verified** (HIGH teammate-link 404; MED search stale-snapshot + mid-typing clobber; LOW aria-live + disabled-chevron). Browser-verified every filter, both search modes, combined AND, multi-poster path, ownership gating; gate green (typecheck 11/11, tests 5/5, build 4/4). `apps/web`+`apps/services` byte-untouched. |
| 2026-07-12 | `feature/navbar-brand-centering` | **Navbar brand polish** (shared `SiteHeader`, all public pages). Logo **h-10→h-9** (smaller); wordmark **text-sm→text-base** (bigger); brand `<Link>` **items-start→items-center** so the CQ mark is centred over the "Career Queue" text. Dropped the eyeballed `pl-1` and added a **measured `-translate-x-[2px]`** on the mark — the opaque glyph in `cq-mark-color.png` (bbox x=52..368 of 400) sits +2px right of the PNG box centre at h-9, so this recentres the *visible* glyph over the text (browser-verified: glyph-centre = text-centre, Δ0.02px). Nav gains **`lg:ml-6`** so the links sit further right off the brand (brand→nav gap 32px→56px). Adversarial review: 0 confirmed (2 sub-pixel NITs refuted). Browser-verified 1320px + 375px (72px bar, 7px top/bottom, no overflow). `SiteHeader.tsx` only — no schema/flags/locks. |
| 2026-07-12 | `feature/recruiter-jobs-list-table` | **Recruiter `/jobs` → structured table + color-coded status badges** (recruiter-only; no schema/flags/locks). Replaced the flat `JobRow` list with a real table: **Job Title · Location · Date Posted · Status** (+ Applicants · Actions), responsive (desktop `<table>` / mobile cards). New reusable `JobStatusBadge` + `JOB_STATUS_META` color-code the tags — **Open (green) · Draft (grey) · Expired (amber) · Closed (red)** — with a WCAG-AA fix scoped to the badge (shared `packages/ui/Badge.tsx` untouched → `apps/web`/`apps/services` unaffected). Widened the page's RSC Prisma read for location (`primaryCity`/`locality`/`workMode`); filter pill "Active"→"Open". Unified `TemplatePicker` onto the same shared map (was drifting: "Active"/grey-Expired). New `JobsTable`/`JobStatusBadge`/`job-list-format`; `JobRow` retired. Adversarial review (correctness clean; 4 a11y fixes applied). Browser-verified (all 4 badge colors AA-legible, filter, dark mode, TemplatePicker consistent). |
| 2026-07-09 | `feature/recruiter-post-a-job` (**P4 · feature complete**) | **Post a Job — Phase 4 (final)** (rich JD editor + Salary Trends + Reach Meter). Additive migration `add_job_description_markdown` (`Job.descriptionMarkdown`). Description field gains a Bold/Bullets/Heading **Markdown toolbar** (no new dep); submit sends `descriptionMarkdown` + a stripped plain `description`. **Seeker render (`apps/web`, additive/zero-regression):** `JobBody` renders the Markdown via the existing sanitized `renderArticleMarkdown` when present, else the unchanged plain-text path (JobBody.tsx + 1 line in the job page; `apps/services` untouched). New read-only `GET /recruiter/jobs/salary-trends` + `/reach` → live sidebar **Salary Trends** panel + **Reach Meter**. +4 tests (API 584). Browser-verified (widgets fetch real data; toolbar; seeker renders sanitized h2/strong/ul with 0 raw markdown; plain job byte-identical). **On pull: `pnpm db:generate && pnpm db:migrate:dev`.** All 4 phases of Post a Job now merged. |
| 2026-07-09 | `feature/recruiter-post-a-job` (**P3**) | **Post a Job — Phase 3** (rich Job Details form + schema; recruiter-only). Additive migration `add_job_type_openings_locality`: `enum JobType` + `Job.jobType` + `openings`/`qualifications`/`internshipDurationMonths` + `model Locality`(FK→City) + `Job.localityId` + `City.localities`. Seeds: **20 functional-areas** (Department dropdown was empty!) + **84 localities**. API DTO/service persist the new fields + `resolveLocalityId` (validate-in-city or find-or-create from a typed name); +3 tests. `PostJobWizard` rebuilt as a single-column sectioned form (Company+Change / Dept / Title / Full-Part-Contract or Internship+duration / Openings / **City→Area dependent dropdown**+Other / Any-Fresher-Experienced / skills / qualifications / salary / Save-draft+Publish). Browser-verified end-to-end (draft persisted all fields incl. localityId; internship variant; 0 console errors). **On pull: `pnpm db:generate && pnpm db:migrate:dev`.** `apps/web`+`apps/services` byte-untouched. Branch stays **In Progress** for P4. |
| 2026-07-09 | `feature/recruiter-post-a-job` (**P2**) | **Post a Job — Phase 2** (template picker + job-type selector; recruiter-only, no schema). New `PostJobFlow` orchestrator: Start → *Create new* → **Job-type selector** (Free / Hot Vacancy / SMB / Internship — Free+Internship live, Hot Vacancy+SMB locked "Upgrade" behind new flags `recruiter.hot_vacancy.enabled` + `recruiter.smb_pack.enabled`, seeded OFF) → form; **or** → **Template picker** (Indeed-style: searchable past jobs, Status+Location filters, deep-copy prefill via `GET /recruiter/jobs/:id`) → form. `PostJobWizard` gains `initialValues` + `jobType` (Internship→INTERN preset; UI-only, not persisted till P3). Browser-verified (selector gating live, template prefill title+desc, 0 console errors). `apps/web`+`apps/services` byte-untouched. Branch stays **In Progress** for P3–P4. |
| 2026-07-09 | `feature/recruiter-post-a-job` (**P1**) | **Post a Job — Phase 1** (nav + route move; recruiter-only, no schema). New `/post-job` page renders the existing `PostJobWizard` (moved off `/jobs/new`, which now 307-redirects); sidebar "Post a Job" item below Jobs; Jobs-page CTA + the previously-dead Dashboard button both → `/post-job`; new `killswitch.recruiter_post_job` (seeded OFF ⇒ LIVE; L2 page 404 / L3 `create()` 503). Runtime-verified (routes, single active nav row, killswitch on/off). `apps/web`+`apps/services` byte-untouched. Branch stays **In Progress** for P2–P4. |
| 2026-07-03 | `feature/navbar-nav-and-brand` | **Navbar polish** (shared `SiteHeader`, all pages). Moved the Jobs/Companies/Career-advice nav links to the **left** next to the logo (was centred) and made them **semibold + 15px**. Added the **"Career Queue" wordmark stacked BELOW the logo mark** (left-aligned, brand navy `--color-primary-600`, nudged right `pl-1` to line up under the mark's glyph). Enlarged the brand (logo h-7→h-10, text 11px→14px) and gave it breathing room by taking the bar **h-16→72px** (~6px above the logo + below the text); updated the two header-height-dependent offsets (`MobileMenu` drawer top/max-h + `Hero` min-height). Browser-verified (72px bar, 6px gaps, no overflow). `SiteHeader` + `MobileMenu` + `Hero`. |
| 2026-07-03 | `feature/srp-search-header` | **Redesigned the job-search page header** (was sparse: bare title + narrow search + a lone "Sort" floating in empty space). Now: a prominent **search bar with an integrated navy Search button** (`SearchInput` gains an opt-in `withButton` segmented variant — icon on mobile, "Search" on sm+, one focus ring, `aria-label="Search jobs"`), a one-tap **"Popular" quick-searches chip row** (new `QuickSearches` → SEO landings), and a balanced controls toolbar (result count left · Filters/Sort right, hairline divider). Browser-verified desktop + mobile (button collapses to icon, chips wrap, no overflow); fixed a `/data-scientist-jobs` 404 in QuickSearches (→ Java) and the same broken link in `RelatedSearches` (→ TypeScript). `apps/web` only. |
| 2026-07-03 | `bugfix/rail-row-congestion` | **Fixed the cramped "roles at other companies" rail rows** (SRP `SrpRail` + job-detail `RelatedRoles`). Real root cause found via in-browser geometry: row padding computed to `0px/0px` because `first:pt-0 last:pb-0` sat on the `<a>` (always the sole → first+last child of its `<li>`), zeroing every row. Moved `py-3.5 first:pt-0 last:pb-0` to the `<li>` (padding now applies), `space-y-1` between the 3 lines, logo 36→40, titles wrap 2 lines (`line-clamp-2`) instead of truncating; company name `font-medium` for a 3-tier hierarchy. Row heights ~59→73–88px. 2-agent design+a11y review: 0 bugs, applied 1 refinement. `apps/web` only. |
| 2026-07-03 | `bugfix/signed-in-header-auth-state` | **Fixed: signed-in seekers saw "Sign in/Register" on public pages** (`/jobs`, `/job/[slug]`, `/companies`) — the shared header resolved auth on the **client** (`GET /auth/me`), flashing/looking logged-out and inconsistent with the always-correct dashboard. Now resolved **server-side**: new cached `getHeaderUser()` (same cookie+JWT path as the dashboard) feeds `SiteHeader` → passes the user to `HeaderAuthActions` (client fetch removed) → correct chrome on first paint. Footer's Account column made auth-aware too (seeker sees Dashboard/Applications/Saved/Notifications, not Sign in/Create account). Guarded `/login` + `/register` → redirect signed-in seekers to `/profile` (the `/` redirect already existed). curl-verified: authed `/jobs` header = Account menu (no Sign-in), footer = dashboard links; `/`,`/login`,`/register` → 307 `/profile`. `apps/web` only. |
| 2026-07-03 | `feature/recruiter-help-support` | Recruiter **Help & Support** (sidebar group): searchable static **FAQ** + **Contact us** form + **Raise a ticket** with a reply thread. Models `SupportTicket`/`SupportTicketMessage`/`SupportContactMessage` (+ enums `SupportTicketStatus`/`SupportTicketCategory`, `NotificationType.SUPPORT_TICKET_UPDATED`, `ProfileAuditAction.SUPPORT_TICKET_STATUS_CHANGED`). API `recruiter-support` (create/reply/close/contact, killswitch 503, cross-user 404) + `admin-support` (`/admin/support/*` under AdminGuard, NOT killswitch-gated) + 2 email kinds + `notifyTicketUpdate` bell producer. Recruiter `/support/{faq,contact,tickets,tickets/[id]}`; admin `/admin/support` queue + detail + contact-messages (isolated subtree, apps/web job-seeker untouched). Flag `killswitch.recruiter_help_support`. Adversarial review: 2 findings fixed. |
| 2026-07-03 | `feature/clickable-job-cards` | **Whole job card is now clickable** (was title-only). Stretched-link: the title link's `::after` overlays the card/row so a click anywhere opens the job; the company link + save/apply/withdraw/expand controls are lifted with `relative z-10` so they still work; clamp/truncate moved to an inner `<span>` so the overlay isn't clipped by `overflow:hidden`. Applied to `srp/JobCard`, `profile/RecommendedJobCard`, `saved-jobs/SavedJobRow`, `applications/ApplicationRow` (overlay scoped to the top row so the expandable timeline stays free), `companies/CompanyOpenings`. Rail/related/hero cards already wrapped the whole card in one `Link`. Browser-verified via `elementFromPoint`: card body → job, save/company/expand → their own action. `apps/web` components only. |
| 2026-07-03 | `feature/job-detail-site-shell` | **Job detail page now carries the shared site chrome**: `/job/[slug]` + its `not-found` were rendering a bare `<main>` under the root layout (no navbar/footer) while the search page got `SiteShell` — so the same detail page looked inconsistent arriving from search or the dashboard. Wrapped both in `SiteShell` + `Container` (matches the search page's header/footer + width). No new rendering penalty (the page already reads cookies for apply/save → already dynamic). Browser-verified: navbar + footer present, 3-col body intact, no overflow. `app/job/[slug]/{page,not-found}.tsx` only. |
| 2026-07-03 | `feature/header-logo-seeker-dashboard` | Header **logo links straight to `/profile` for signed-in seekers** (no `/`→307→`/profile` hop): `SiteHeader` now reads the verified session (`Promise.all` with the Google-flag read) and sets the brand link + aria-label from `role === 'CANDIDATE'`. Role comes from the JWT claims (no DB lookup); anon + recruiters/admins keep home. No dynamic penalty (SiteHeader is only on already-dynamic home + SRP). curl-verified: authed header logo `href=/profile` (aria "dashboard"), anon `href=/` (aria "home"). Footer logo left at `/` (conventional). `SiteHeader.tsx` only. |
| 2026-07-03 | `feature/home-redirect-signed-in-seekers` | Signed-in **seekers no longer see the marketing home**: `apps/web/app/page.tsx` reads the verified session and `redirect()`s `role === 'CANDIDATE'` users to `/profile` before rendering — any visit to `/` (typed URL, logo, client nav, post-login) bounces to the dashboard. Anon + crawlers keep the full home (SEO); recruiters/admins keep it too. Stale cookie falls through (no loop). curl-verified: authed `/` → 307 `/profile`, anon `/` → 200, authed `/jobs` + `/profile` → 200. `apps/web/app/page.tsx` only — no schema/flags/locks. |
| 2026-07-03 | `feature/job-search-srp-redesign` | **Job-search SRP redesign** (`/jobs` + all `[...path]` SEO SRPs): shared site shell (reuses home `SiteHeader`/`SiteFooter`) on every search page + **signed-in user menu** (avatar → Dashboard/Saved/Applications/Alerts/Settings/Sign out; auth resolved client-side via `/auth/me`, no server cookie read → ISR-safe). 3-column `SrpShell` (filters · results · rail) with a prominent search toolbar + removable **active-filter chips**. Redesigned `JobCard` (+`CompanyLogo`, **real city names**, snippet, consolidated `formatSalaryLpa`/`formatExperienceMonths`/`postedAgo`). New `SrpRail` (job-alert CTA + one-role-per-company "roles at other companies"). Fixed 768–1023px filter dead-zone. Adversarial review: 3 findings fixed (logout hardening, rail `h2`, Hire-CTA hover AA). `apps/web` only — no schema/migration/flags/locks. |
| 2026-07-02 | `feature/job-detail-layout` | Redesigned `/job/[slug]` into a full-width hero card + 3-column grid (left: **Job overview** facts + **About-company** card; center: description; right: NEW **"Similar roles at other companies"** — shared-skill match at OTHER companies, with logos). New `JobHero`/`JobOverviewCard`/`AboutCompanyCard`/`RelatedRoles` + `lib/job/format.ts`; retired `JobHeader`/`JobMeta`/`SimilarJobs`; JSON-LD now emits real `employmentType`. Browser-verified 3-col/sticky/logos + mobile stack. `packages/ui` +Clock icon (append). |
| 2026-07-02 | `feature/seeker-dashboard-polish` | Seeker dashboard UI overhaul (all 10 pages): shared `PageHeader`/`ContentCard`/`Pagination`/`EmptyState` primitives → card-based restyle of applications/saved-jobs/alerts/profile-subpages/settings; expandable **application status timeline** from `Application.statusHistory` (read-only, no schema); scrollable status-filter chips with counts; mobile row stacking; **redesigned recommended-job cards** (company logo 44px + object-contain, resolved city names, ₹N–M LPA, skills, posted-age). Two adversarial reviews → 30 confirmed findings fixed (ARIA tablist→links, IST date hydration, contrast, corner clipping, legacy-row timeline). apps/web only — no schema, no flags, no locks |
| 2026-07-02 | `bugfix/signin-popup-redirect` | Seeker sign-in now lands on the dashboard: navbar `AuthModal` pushes `/profile` after login (was close+refresh-in-place, which also raced and swallowed navigation); `/login` bare-`/` fallback → `/profile` (matches Google OAuth fallback); `?next=` deep links unchanged. Browser-verified all 3 paths. apps/web auth components only |
| 2026-07-02 | `feature/recruiter-billing` | Recruiter **Plans & Billing** (Razorpay prepaid, company-scoped): `PaymentOrder`/`PaymentWebhookEvent`/`CompanyBillingProfile` + `Subscription.companyId` + `SubscriptionPlan.audience` + GST-extended `SubscriptionInvoice`; API `recruiter-billing` + `POST /webhooks/razorpay`; `/plans` + `/billing` pages + sidebar Billing group; middleware L1 on `subscription.system.enabled` (reused, no new key). Adversarial review: 14 findings fixed. apps/web untouched |
| 2026-07-01 | `feature/recruiter-user-management` | Recruiter **Users / Team management**: `RecruiterRole` (Owner/Admin/Member) + per-module permissions + `RecruiterInvite`; `recruiter-users` API (invite/revoke/role+perms/remove/accept/preview), `/users` panel + public `/accept-invite/[token]`; soft-remove + re-login block + reactivate-on-reinvite; `killswitch.recruiter_user_management`; registration now new-company-only (creator = Owner), join is invite-only. Adversarial review: 5 findings fixed. apps/web untouched |
| 2026-07-01 | `feature/recruiter-change-password` | Recruiter **Settings** sidebar group + **Change Password**: `POST /auth/recruiter/change-password` (verify current → new Argon2id hash → revoke all sessions + `RECRUITER_PASSWORD_CHANGE` audit in one txn → re-mint current device), `ChangePasswordDto` + `RecruiterPasswordService`, `/settings/change-password` page + `ChangePasswordForm`; moved `/notification-settings` → `/settings/notification-settings` (old path redirects); `killswitch.recruiter_change_password`. apps/web untouched |
| 2026-06-30 | `feature/recruiter-topbar-and-toggle-fix` | Recruiter UI follow-up: fixed invisible notification toggles (added missing `@source` for `@jobportal/ui` in recruiter `globals.css`); moved company logo + name + `KycStatusBadge` from the dashboard header into the `(authed)` top bar (shows on every page). Browser-verified. |
| 2026-06-30 | `feature/recruiter-notifications` | Recruiter notification settings + top-bar bell: `Notification`+`RecruiterNotificationPreference` models + `NotificationType` enum, `recruiter-notifications` API (list/unread-count/read/read-all + prefs), producers in `applications.apply()`+`admin-kyc.review()`, `/notification-settings` page + `NotificationBell` (polling), `killswitch.recruiter_notifications`. apps/web untouched |
| 2026-06-30 | `feature/recruiter-company-verification` | Recruiter Company Verification (KYC): `CompanyKyc`+`KycDocument`, `recruiter-kyc`+`admin-kyc` API, `/kyc` tab + status badge, `/admin/kyc-review`, `killswitch.recruiter_kyc` |
| 2026-06-21 | `feature/recruiter-profile-editing` | Recruiter profile + company editing + logo upload |
| 2026-06-21 | `feature/recruiter-single-email` | Recruiter single-email migration |
| 2026-06-20 | `feature/google-oauth` | Google OAuth login |

---

*Stale rows are noise. If something here has been merged or abandoned, clean it up — anyone can.*
