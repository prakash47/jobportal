{
  "name": "@jobportal/observability",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Sentry + PostHog helpers (Phase 1 item 18).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./scrub": "./src/scrub.ts",
    "./is-telemetry-enabled": "./src/is-telemetry-enabled.ts"
  },
  "scripts": {
    "lint": "echo 'no lint configured yet'",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .turbo dist"
  },
  "dependencies": {
    "@jobportal/feature-flags": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "vitest": "^3.0.0"
  }
}
