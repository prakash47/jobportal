// @jobportal/search — Elasticsearch 9 client + indexer helpers.
// Replaces the SRS's Meilisearch reference per CLAUDE.md §1 (long-term scalability).
// Implementations follow in feature/elasticsearch-integration.

export {};
