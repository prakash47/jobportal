{
  "name": "@jobportal/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types and Zod schemas.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "echo 'no lint configured yet'",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .turbo dist"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
