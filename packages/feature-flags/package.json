{
  "name": "@jobportal/feature-flags",
  "version": "0.1.0",
  "private": true,
  "description": "Backend-controlled feature flag system (SRS §7).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "echo 'no lint configured yet'",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .turbo dist"
  },
  "dependencies": {
    "@jobportal/db": "workspace:*",
    "@jobportal/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
