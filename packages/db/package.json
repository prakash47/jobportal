{
  "name": "@jobportal/db",
  "version": "0.1.0",
  "private": true,
  "description": "Prisma 7 schema, client, and query helpers.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate deploy",
    "db:migrate:dev": "prisma migrate dev",
    "db:studio": "prisma studio",
    "lint": "echo 'no lint configured yet'",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .turbo dist generated"
  },
  "dependencies": {
    "@prisma/client": "^7.4.0",
    "@jobportal/types": "workspace:*"
  },
  "devDependencies": {
    "prisma": "^7.4.0",
    "dotenv": "^16.4.0",
    "typescript": "^5.9.0"
  }
}
