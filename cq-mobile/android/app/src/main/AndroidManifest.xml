<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required for all network calls. Flutter adds this automatically to
         DEBUG/PROFILE builds only, so without it here every request in a
         RELEASE build fails with "Connection failed". -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Backup is OFF on purpose. The session lives in cookies, and the refresh
         cookie is a 30-day credential. With backup enabled Android copies app
         data to the user's Google account, which would carry a live session off
         the device and restore it onto another one. All three attributes are
         needed: allowBackup and fullBackupContent cover API <= 30,
         dataExtractionRules covers 31+. -->
    <application
        android:label="Career Queue"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false">
        <!-- Portrait only, matching iOS. Every screen was designed and reviewed
             at phone width and nothing was laid out for landscape; rotating
             produced overflowing rows rather than a wide layout. Remove the
             screenOrientation lock the day those layouts exist. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:screenOrientation="portrait"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Some budget Android GPUs (Vivo/BBK/Mali, common in India) render a
             blank first frame under Impeller — the launch screen comes up empty.
             Fall back to the Skia renderer so the splash and the whole UI paint
             reliably. Visual output is identical; this only swaps the engine.
             Remove this line to re-enable Impeller once the device supports it. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Required by url_launcher for the company-website links. Android 11+
             package visibility hides other apps unless they are declared here,
             so without this canLaunchUrl reports false even with a browser
             installed — and it fails that way only on real devices/release
             builds, not necessarily in a debug emulator run. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="http"/>
        </intent>
    </queries>
</manifest>
