<?xml version="1.0" encoding="utf-8"?>
<!--
    Android 12+ (API 31) backup/transfer rules.

    Everything is excluded. The app's stored state is the login session (a
    30-day refresh cookie in the keystore) plus cached feed data, and none of it
    should travel to Google's servers or to a new device. There is nothing here
    a user would miss on reinstall: signing in again restores the whole profile
    from the server.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="file" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="file" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
